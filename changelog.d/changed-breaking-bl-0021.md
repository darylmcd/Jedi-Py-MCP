- **Changed — BREAKING:** Closed-set tool parameters (`call_hierarchy`/`type_hierarchy` `direction`, `get_diagnostics` `severity_filter`, `docstring_sync` `style`, `generate_code` `kind`, `structural_search` `language`, `change_signature` `op`) now advertise `enum` (or `const`) in `inputSchema` and reject out-of-set, differently-cased, or padded values — and the former `type_hierarchy` `callers`/`callees` aliases — with a validation error naming the parameter; 0-based `line`/`character`/`start_*`/`end_*` position parameters advertise and enforce `minimum: 0` (bl-0021).
