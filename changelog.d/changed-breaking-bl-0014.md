- **Changed — BREAKING:** a nonexistent `root_path`/`file_path` no longer reads as a clean result — `dead_code_detection` / `unused_symbol_sweep` report a `phase="resolve"` `scan_failures` entry, `get_symbol_outline` rejects a missing `file_path`/`file_paths` member/`root_path` with `[INVALID_INPUT]`, and `get_type_coverage` returns `null` coverage percentages (not `100.0`) when no file was scanned.
