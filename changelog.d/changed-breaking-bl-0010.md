- **Changed — BREAKING:** `get_symbol_outline` returns `{items, total_count, offset, truncated, total_nodes, returned_nodes}` instead of a bare list and caps workspace scans at 250 nodes by default (`max_nodes`); `code_metrics` (now sorted by cyclomatic complexity, highest first), `get_module_dependencies`, `search_symbols` and `dead_code_detection` apply documented default limits (200/500/200/200) and report `truncated`.
