# Backlog sweep plan — 20260924T131340Z_backlog-remediate

Generated by `/backlog-remediate`. 15 initiative(s) selected off `ai_docs/backlog.md` (snapshot 2026-09-24T13:06:33Z).

<!-- BSWEEP:STATUS-TABLE BEGIN — generated from state.json; do not edit by hand -->
## Status (generated)

| # | id | status | PR | rows closed |
|---|----|--------|----|-------------|
| 1 | bl-0002 | merged | [#112](https://github.com/darylmcd/Jedi-Py-MCP/pull/112) | bl-0002 |
| 2 | bl-0004 | merged | [#113](https://github.com/darylmcd/Jedi-Py-MCP/pull/113) | bl-0004 |
| 3 | bl-0005 | merged | [#118](https://github.com/darylmcd/Jedi-Py-MCP/pull/118) | bl-0005 |
| 4 | bl-0006 | merged | [#111](https://github.com/darylmcd/Jedi-Py-MCP/pull/111) | bl-0006 |
| 5 | bl-0007 | merged | [#121](https://github.com/darylmcd/Jedi-Py-MCP/pull/121) | bl-0007 |
| 6 | bl-0001 | merged | [#110](https://github.com/darylmcd/Jedi-Py-MCP/pull/110) | bl-0001 |
| 7 | bl-0008 | merged | [#114](https://github.com/darylmcd/Jedi-Py-MCP/pull/114) | bl-0008 |
| 8 | bl-0010 | merged | [#123](https://github.com/darylmcd/Jedi-Py-MCP/pull/123) | bl-0010 |
| 9 | bl-0011 | merged | [#119](https://github.com/darylmcd/Jedi-Py-MCP/pull/119) | bl-0011 |
| 10 | bl-0012 | merged | [#125](https://github.com/darylmcd/Jedi-Py-MCP/pull/125) | bl-0012 |
| 11 | bl-0013 | merged | [#128](https://github.com/darylmcd/Jedi-Py-MCP/pull/128) | bl-0013 |
| 12 | bl-0014 | pending | — | bl-0014 |
| 13 | bl-0015 | merged | [#117](https://github.com/darylmcd/Jedi-Py-MCP/pull/117) | bl-0015 |
| 14 | bl-0016 | merged | [#127](https://github.com/darylmcd/Jedi-Py-MCP/pull/127) | bl-0016 |
<!-- BSWEEP:STATUS-TABLE END -->

## Initiatives

### 1. bl-0002

| Field | Content |
|---|---|
| Route | deepen |
| Diagnosis | `tool_runtime.py:259-260` `tool_error_boundary` catches only `BackendError`; any other exception reaches the MCP SDK as `UnexpectedToolError` → caller sees bare `Error executing tool <name>` and stderr logs a traceback. Acceptance repros raise plain builtins: `util/shared.py:106-108` (`validate_identifier` ValueError, via `_validate_params` `tool_runtime.py:174`), `tools/analysis/type_stubs.py:155-158` (FileNotFoundError), `tools/refactoring/code_actions.py:23-24` (`_pick_code_action` ValueError on empty actions, contradicting `tool_registry.py:605`). `errors.py` has only redacted `BackendError` subclasses — no caller-safe channel. |
| Approach | (1) `errors.py`: `class ToolInputError(ValueError)`, `code: ClassVar[str] = "INVALID_INPUT"`; docstring: message is caller-safe, surfaced verbatim, names the parameter + reason. Not a `BackendError` (redaction contract unchanged); `ValueError` base keeps existing `except ValueError` handlers/tests working. bl-0003 reuses it. (2) `tool_runtime.py`: `except ToolInputError` arm before the `BackendError` arm → `_translate_input_error` (DEBUG log, `extra={event: tool_input_error, tool_name, error_code}`, no traceback/failure id) returning `ToolError(f"[INVALID_INPUT] {exc}")`; convert `:166,:189` raises. (3) `util/shared.py:33,106,108,122` → `ToolInputError`. (4) `type_stubs.py:152-158` → `ToolInputError`; stub arm reads `Stub file not found: <path>` + hint that `stub_file` defaults to the adjacent `.pyi`. (5) `code_actions.py` `apply_code_action`: empty `actions` → `RefactorResult(edits=[], files_affected=[], description="No code actions available at the requested position", applied=False)` (mirror `organize_imports` `:101-104`); `:37` (no title match — list available titles) and `:65` → `ToolInputError`. |
| Scope | Production 5: `errors.py`, `tool_runtime.py`, `util/shared.py`, `tools/analysis/type_stubs.py`, `tools/refactoring/code_actions.py` (all under `src/python_refactor_mcp/`). Over target — `defect-forced-companion`: the new boundary arm matches only `ToolInputError`, and each Acceptance repro raises from its own module (`shared.py:106`, `type_stubs.py:158`, `code_actions.py:24`); with only errors.py+tool_runtime.py changed all three still return the bare error. Tests 3: `tests/unit/test_error_boundary.py` (retarget `:427`, `:472` to `ToolError` `^\[INVALID_INPUT\] `; new: surfaced verbatim, no ERROR record), `tests/unit/test_refactoring_tools.py` (empty actions → empty edits; unmatched title → `ToolInputError`), `tests/unit/test_type_stub_freshness.py` (missing `.pyi` → `Stub file not found`). Fragment `changelog.d/fixed-bl-0002.md`. |
| Tool policy | edit-only |
| Estimated context cost | 55000 |
| Risks | Probe: 46 `raise ValueError/FileNotFoundError` in 19 prod files; caller-input sites in 8 other modules (structural.py, hierarchy.py, rename.py, outline.py, diagnostics.py, type_users.py, search/_helpers.py, server.py:239) stay opaque by design → follow-on row (deps bl-0002). Converting all 13 files would exceed fanout 10. `validate_workspace_path` message now reaches the caller (caller's own paths — acceptable). `check_type_stub_freshness` raises `ToolInputError` (ValueError) not FileNotFoundError — only caller `tool_registry.py:343`. Non-Tool/non-Backend exceptions stay opaque (intended). Siblings bl-0007/bl-0012/bl-0014 dependOn this. `tool_runtime.py` hotspot; bl-0008 edits nearby logging only. fanoutEstimate 5. |
| Validation | Per edit: pyright on changed files; `pytest tests/unit/test_error_boundary.py tests/unit/test_refactoring_tools.py tests/unit/test_type_stub_freshness.py tests/unit/test_shared_utils.py -q`. Repro: `rename_symbol{new_name:"   "}` → `[INVALID_INPUT] …new_name`; stub freshness with no `.pyi` → `Stub file not found`; `apply_code_action` on a blank line → empty edits; no "unexpected exception" on stderr. `just ci` with `CHANGELOG_BASE_REF=<baseRef>` (full run serialized per addenda). |
| Performance review | N/A — correctness fix, no hot-path changes. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | - **Fixed:** Caller-input errors (invalid identifiers, out-of-workspace paths, missing stub files, unmatched code-action titles) now return an `[INVALID_INPUT]` tool error with the reason instead of a bare `Error executing tool <name>`; `apply_code_action` returns an empty result when no actions exist. Backend-failure redaction is unchanged. |
| Backlog sync | Close rows: [bl-0002]. Mark obsolete: []. |

### 2. bl-0004

| Field | Content |
|---|---|
| Route | direct |
| Diagnosis | `backends/pyright_lsp.py:1190` `if isinstance(result, dict) and "range" not in result: return None` treats Pyright's bare `Range` reply (`{start,end}`) as `{defaultBehavior}` and returns null; `rename_symbol` at the same position succeeds. Confirms the row. |
| Approach | Acceptance (verbatim): (1) `prepare_rename` on the `User` class name in the integration fixture returns its identifier range + placeholder `User`. (2) unit test feeds each of the three LSP reply shapes (Range, {range,placeholder}, {defaultBehavior}) through the parser. (3) integration test no longer accepts `None` as success. Fix in `prepare_rename`: dict with `range` → use it + placeholder; dict with `start`/`end` → bare Range; dict with `defaultBehavior` → derive the identifier range at the requested position from the source line (word boundaries) and use it; anything else → None. |
| Scope | Production (1): `src/python_refactor_mcp/backends/pyright_lsp.py` (`prepare_rename` only). Tests (2): `tests/unit/test_pyright_lsp.py`, `tests/integration/test_end_to_end.py`. |
| Tool policy | edit-only |
| Estimated context cost | 25000 |
| Risks | bl-0005 and bl-0007 edit other functions of `pyright_lsp.py` (hotspot) — serialize. Integration test requires `RUN_MCP_INTEGRATION=1` (in `just ci`). fanoutEstimate 0 (no signature change). |
| Validation | Regression test first (red), then `just ci` with `CHANGELOG_BASE_REF=<baseRef>`; per-edit `.venv/Scripts/python.exe -m pytest <test file> -q` + pyright on changed files. Fragment `changelog.d/fixed-bl-0004.md`. |
| Performance review | N/A — correctness fix, no hot-path changes. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | - **Fixed:** `prepare_rename` now returns the identifier range for Pyright's bare-`Range` reply instead of `null`, and derives a range for `{defaultBehavior}` replies. |
| Backlog sync | Close rows: [bl-0004]. Mark obsolete: []. |

### 3. bl-0005

| Field | Content |
|---|---|
| Route | deepen |
| Diagnosis | Locked Pyright 1.1.411 answers `-32601 Unhandled method` for selectionRange/inlayHint/semanticTokens; `backends/pyright_lsp.py:1022-1024`, `:1244-1246`, `:1303-1305` map that to `return []`. `start()` (`:354-392`) and restart (`:243-265`) drop `initialize` `result.capabilities`. `apply_type_annotations` (`tools/refactoring/type_annotations.py:64,88-94`) then reports "No inferable type annotations found" (false negative). Integration tests `tests/integration/test_end_to_end.py:407-429,485-515` assert only `isinstance(list)`. |
| Approach | Explicit unsupported error (hiding tools rejected: tool list is static at registration while capabilities are per-workspace, and removal breaks the public contract; AST fallback = second shape → spin-off). (1) `errors.py`: `LspFeatureUnsupportedError(PyrightError)`, `code="LSP_UNSUPPORTED"`, ClassVar `caller_summary` saying the running Pyright does not implement this LSP feature, no data available, do not retry — reaches callers via the existing boundary. (2) `pyright_lsp.py`: extract the duplicated initialize handshake of `start()`/`restart()` into `_initialize_session()` storing `self._server_capabilities: JSONDict \| None` (None in `__init__`); `_require_capability(provider_key, method)` raises when capabilities are recorded and `selectionRangeProvider`/`inlayHintProvider`/`semanticTokensProvider` is falsy (skipped when None); in the 3 methods call it before `_request` and replace the unhandled-method `return []` with `raise LspFeatureUnsupportedError`. Other 10 unhandled branches untouched. (3) `apply_type_annotations`: no code change — the raise propagates and states why. |
| Scope | Production 2: `src/python_refactor_mcp/errors.py`, `src/python_refactor_mcp/backends/pyright_lsp.py`. Tests 3 (extended): `tests/unit/test_pyright_lsp.py` (capabilities recorded; absent capability raises without sending; -32601 raises for all 3; existing `:600-640` positives stay green via None path), `tests/unit/test_refactoring_tools.py` (`apply_type_annotations` propagates the error), `tests/integration/test_end_to_end.py` (non-empty payload OR `is_error` with `[LSP_UNSUPPORTED]`, never bare `isinstance(list)`). Fragment `changelog.d/fixed-bl-0005.md`. |
| Tool policy | edit-only |
| Estimated context cost | 40000 |
| Risks | Fanout: callers `tools/analysis/tokens.py:44,60`, `tools/navigation/outline.py:211`, `type_annotations.py:64` only propagate → ripple 2. Behavior change (tool error instead of `[]`) — mention in PR body; `fixed-*` (no input newly rejected). Pyright pin `>=1.1.409` so integration asserts must accept real data. Every `BackendError` logs as ERROR with failure id (bl-0002/bl-0008 own the boundary). Shares `pyright_lsp.py` with bl-0004/bl-0007, `errors.py` with bl-0002 — serialized by graph. Spin-offs: silent `[]` at `pyright_lsp.py:754,964,980,996,1118,1138,1368,1405,1428`; AST fallback for selection_range; apply_type_annotations without inlay hints. fanoutEstimate 2. |
| Validation | pyright on both files; `pytest tests/unit/test_pyright_lsp.py tests/unit/test_refactoring_tools.py tests/unit/test_error_boundary.py -q`; `RUN_MCP_INTEGRATION=1` confirms `[LSP_UNSUPPORTED]` under 1.1.411. `just ci` with `CHANGELOG_BASE_REF=<baseRef>` (full run serialized per addenda). |
| Performance review | N/A — one dict lookup before three already-failing requests. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | - **Fixed:** `selection_range`, `get_inlay_hints`, `get_semantic_tokens` and `apply_type_annotations` return an explicit `[LSP_UNSUPPORTED]` error when the running Pyright does not implement the LSP method, instead of a silent empty result; the client now records Pyright's advertised capabilities. |
| Backlog sync | Close rows: [bl-0005]. Mark obsolete: []. |

### 4. bl-0006

| Field | Content |
|---|---|
| Route | direct |
| Diagnosis | `backends/rope_backend.py:996-1003` builds a dict referencing `rope_generate.create_class/create_function/create_variable`, which do not exist in rope 1.14 (`rope/contrib/generate.py` exposes `create_generate(kind, project, resource, offset, goal_resource=None)` at :32, `create_module(project, name, sourcefolder)` :54, `create_package` :65); the dict literal raises `AttributeError` before `kind` is consulted, so every kind fails. The `pyright: ignore[reportAttributeAccessIssue]` masks it. |
| Approach | Acceptance (verbatim): (1) preview for `kind="function"` at an unresolved call returns a stub-definition edit. (2) each of `class`, `function`, `variable`, `module`, `package` is exercised against real rope (not mocked). (3) no `# pyright: ignore[reportAttributeAccessIssue]` remains in the generator table. Fix: validate `kind` against the five names, then `rope_generate.create_generate(kind_lower, project, resource, offset).get_changes()` for all kinds (the `_Generate` subclasses cover module/package from a usage site), feed the ChangeSet to `_build_result`. Unknown kind keeps the existing error. |
| Scope | Production (1): `src/python_refactor_mcp/backends/rope_backend.py` (`generate_code` only). Tests (1): `tests/unit/test_rope_backend.py`. |
| Tool policy | edit-only |
| Estimated context cost | 25000 |
| Risks | Module/package generation creates files — preview may show only text edits until bl-0013 lands (file-op entries); acceptance for those kinds is that rope returns a change set without error and apply creates the file. `rope_backend.py` is a hotspot shared with bl-0012/bl-0013 — serialize. fanoutEstimate 0. |
| Validation | Regression test first (red), then `just ci` with `CHANGELOG_BASE_REF=<baseRef>`; per-edit `.venv/Scripts/python.exe -m pytest <test file> -q` + pyright on changed files. Fragment `changelog.d/fixed-bl-0006.md`. |
| Performance review | N/A — correctness fix, no hot-path changes. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | - **Fixed:** `generate_code` now uses rope's `create_generate` factory; every kind previously failed with `AttributeError` against rope 1.14. |
| Backlog sync | Close rows: [bl-0006]. Mark obsolete: []. |

### 5. bl-0007

| Field | Content |
|---|---|
| Route | deepen |
| Diagnosis | `backends/pyright_lsp.py:1389-1410` sends `pyright.createtypestub` with `[package_name(, output_dir)]` and returns True on no `error` key. Pyright 1.1.411 `CreateTypeStubCommand.execute` (`.venv/Lib/site-packages/pyright/dist/dist/pyright-internal.js`) needs `[rootUri, importName, fileUri?]`, no-ops on <2 args, treats our `output_dir` as the import name, catches every failure into showMessage and always returns null — success is undetectable. Output is fixed at `stubPath ?? <root>/typings/<top>`. The CLI `--createstub <IMPORT>` is headless: prints `Type stub was created…` (exit 0) or `Error occurred when creating type stub: Import 'x' could not be resolved` (non-zero). `tools/analysis/type_stubs.py:14-26` and `tool_registry.py:329-334` return bool; description omits location/no-preview. |
| Approach | (1) `tools/analysis/type_stubs.py`: rewrite `create_type_stubs(config, package_name, output_dir)`: validate dotted identifier (bl-0002 `ToolInputError`); output root = `output_dir` or `<workspace>/typings`, checked with `validate_workspace_path` (`util/shared.py:112`); refuse existing `<root>/<top>`; run `asyncio.create_subprocess_exec(sys.executable, "-m", "pyright", "--createstub", pkg, "--pythonpath", str(config.python_executable), cwd=<tempdir>)` (argv list, mirror `tools/refactoring/lint_fix.py:50-60`); non-zero exit → caller-safe error carrying Pyright's message; exit 0 with no `.pyi` under `<tempdir>/typings/<top>` → caller-safe error; else move tree to output root and return created paths. (2) `models.py`: `TypeStubCreationResult{package_name, output_dir, files: list[str]}` near `TypeStubFreshnessResult` (`:475`). (3) `tool_registry.py:329-334`: return the model; docstring: writes immediately — no preview; default `<workspace>/typings/<package>`; `output_dir` workspace-bounded; existing target refused; unresolvable import errors. (4) delete dead `create_type_stub` in `pyright_lsp.py:1389-1410`. (5) `docs/tool-reference.md:26` return type. |
| Scope | Production 4: `src/python_refactor_mcp/tools/analysis/type_stubs.py`, `tool_registry.py`, `models.py`, `backends/pyright_lsp.py` (deletion only). Tests 2: new `tests/unit/test_create_type_stubs.py` (subprocess mocked: bad identifier, out-of-workspace dir, existing target, non-zero exit message, exit 0 no stubs, happy path), `tests/integration/test_end_to_end.py` (real CLI `parso` into tmp workspace → existing `.pyi` paths; bad/unknown name errors). Doc 1: `docs/tool-reference.md`. Fragment `changelog.d/changed-breaking-bl-0007.md`. |
| Tool policy | edit-only |
| Estimated context cost | 55000 |
| Risks | dependsOn bl-0002 (reasons must reach caller). LSP route rejected: audit tried `[<ws uri>,"parso"]` live → null, no `typings/`. CLI child must use `--pythonpath <workspace interpreter>`; integration fixture interpreter must import `parso` (else pick a fixture-installed package); pyright wrapper may download node on cold cache. BREAKING (operator OK'd 2026-09-24): bool → model, garbage names error, `output_dir` bounded — PR-body callout. bl-0016 relies on this description containing "no preview". Fanout: `rg create_type_stub` → pyright_lsp.py:1389, type_stubs.py:17-26, tool_registry.py:329, `tools/analysis/__init__.py` re-export (unchanged); ripple 4. Flag: `PATH_PARAMS` (`tool_runtime.py:32-39`) lacks `*_dir` params. |
| Validation | pyright on changed files; `pytest tests/unit/test_create_type_stubs.py -q`; `RUN_MCP_INTEGRATION=1` end-to-end. Acceptance: `parso` → existing `.pyi` paths; bad/unknown names → reasoned error; description states location + immediate write. `just ci` with `CHANGELOG_BASE_REF=<baseRef>` (full run serialized per addenda). |
| Performance review | N/A — one on-demand CLI subprocess per call. |
| CHANGELOG category | Changed — BREAKING |
| CHANGELOG entry (draft) | - **Changed — BREAKING:** `create_type_stubs` now runs the Pyright CLI, returns the created `.pyi` paths instead of `true`, writes to `<workspace>/typings/<package>` by default (or a workspace-bounded `output_dir`), and errors when the import cannot be resolved or no stubs are produced. |
| Backlog sync | Close rows: [bl-0007]. Mark obsolete: []. |

### 6. bl-0001

| Field | Content |
|---|---|
| Route | direct |
| Diagnosis | `tools/search/dead_code.py:59-63` calls `pyright.get_references(..., False)` (declaration excluded) but `:76` keeps a symbol only when `same_file_count > 1`, i.e. it assumes the declaration was counted — a once-referenced symbol is reported dead. Confirms the row. |
| Approach | Acceptance (verbatim): (1) A private helper defined once and called once in the same file is NOT reported dead; a helper with zero call sites still is (fixture pair in `tests/unit/test_search_tools.py`). (2) `dead_code_detection` on `src/` of this repo drops from 306 items to a number a maintainer can review; the remaining items are spot-checked live. (3) The threshold and the `include_declaration` argument are documented together at the call site so they cannot drift apart again. Fix: keep `include_declaration=False` and change the threshold to `same_file_count >= 1` (or equivalently any reference), with a comment binding the two; also guard the declaration-position self-reference Pyright may return even with include_declaration=False (skip a ref whose range equals `symbol_range`). |
| Scope | Production (1): `src/python_refactor_mcp/tools/search/dead_code.py`. Tests (1): `tests/unit/test_search_tools.py`. |
| Tool policy | edit-only |
| Estimated context cost | 25000 |
| Risks | bl-0014 and bl-0010 may also edit `dead_code.py` (serialize via conflict graph). `unused_symbol_sweep` (`tools/search/unused_symbols.py`) may share the same threshold bug — check and flag as spin-off only if it does; do not fix inline. Fanout: no signature change, fanoutEstimate 0. |
| Validation | Regression test first (red), then `just ci` with `CHANGELOG_BASE_REF=<baseRef>`; per-edit `.venv/Scripts/python.exe -m pytest <test file> -q` + pyright on changed files. Fragment `changelog.d/fixed-bl-0001.md`. |
| Performance review | N/A — correctness fix, no hot-path changes. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | - **Fixed:** `dead_code_detection` no longer reports every once-referenced symbol as dead; the reference threshold now matches the declaration-excluded reference query. |
| Backlog sync | Close rows: [bl-0001]. Mark obsolete: []. |

### 7. bl-0008

| Field | Content |
|---|---|
| Route | direct |
| Diagnosis | `tool_runtime.py:218-233` logs `tool_backend_failure` with a structured `extra` (`failure_id`, `exception_types`, `traceback_locations`), but nothing under `src/` configures a handler/formatter (`rg basicConfig|StreamHandler|Formatter src` → 0 hits), so stderr shows only the `%s` message and the `extra` fields are lost. |
| Approach | Acceptance (verbatim): (1) a forced backend failure's stderr line carries `failure_id`, `exception_types`, and `traceback_locations`. (2) output stays free of request paths/payloads (redaction contract of `_safe_failure_diagnostics` unchanged). Fix: in `__main__.py` install a stderr `logging.Handler` on the package logger (`python_refactor_mcp`) with a formatter that appends the whitelisted `extra` keys of `tool_backend_failure` records (only those keys; never `args`/payloads). Keep stdout untouched (stdio transport). |
| Scope | Production (1): `src/python_refactor_mcp/__main__.py` (formatter class lives there; no new module). Tests (1): `tests/unit/test_main.py`. |
| Tool policy | edit-only |
| Estimated context cost | 25000 |
| Risks | Must not write to stdout (MCP stdio). Avoid duplicate handlers if `main()` is called twice in tests (idempotent install). Check whether the MCP SDK already configures root logging to avoid double lines. fanoutEstimate 0. |
| Validation | Regression test first (red), then `just ci` with `CHANGELOG_BASE_REF=<baseRef>`; per-edit `.venv/Scripts/python.exe -m pytest <test file> -q` + pyright on changed files. Fragment `changelog.d/fixed-bl-0008.md`. |
| Performance review | N/A — correctness fix, no hot-path changes. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | - **Fixed (observability):** backend-failure stderr lines now include the failure id, exception types, and traceback locations recorded for each `Failure ID`. |
| Backlog sync | Close rows: [bl-0008]. Mark obsolete: []. |

### 8. bl-0010

| Field | Content |
|---|---|
| Route | deepen |
| Diagnosis | `server.py:172-174` sets `effective_limit = 500` for workspace outlines; `tools/navigation/outline.py:188` `apply_limit` slices top-level roots only, children unbounded (500 roots / 9,933 nodes / 7.5 MB); return type is a bare `list[SymbolOutlineItem]` (`server.py:167`, `outline.py:131`) with no room for `truncated`. `code_metrics` (`tool_registry.py:1298-1313` → `tools/metrics/complexity.py:60-109`) and `get_module_dependencies` (`tool_registry.py:1316-1332` → `tools/metrics/dependencies.py:226-306`) take no limit (339 KB / 652 KB); `CodeMetricsResult` (`models.py:535`) / `DependencyGraph` (`:555`) lack `truncated`. `search_symbols` (`tool_registry.py:1185`) and `dead_code_detection` (`:1224`) support `limit`/`truncated` but default `None`. |
| Approach | One shape: default payload bounds + `truncated`. (a) `models.py`: `SymbolOutlineResult(Paginated[SymbolOutlineItem])` + `total_nodes`, `returned_nodes`; `CodeMetricsResult.truncated=False`; `DependencyGraph.truncated=False`, `total_dependencies=0` (defaults keep construction sites compiling). (b) `outline.py`: `max_nodes` param; after sort/offset/limit walk roots counting 1+descendants, stop at budget, prune the crossing root's children depth-first; return `SymbolOutlineResult`. `limit` keeps meaning roots. (c) `server.py:158-187`: return `SymbolOutlineResult`; `max_nodes` defaults to 250 for workspace scans (mirror `:172-174`). (d) `complexity.py`/`dependencies.py`: internal `limit=None` (full graph still used by `metrics/architecture.py:38`, `refactoring/circular_imports.py:421`); code_metrics computes aggregates before capping, sorts by cyclomatic desc then path/line; deps caps only `dependencies`. (e) `tool_registry.py`: `code_metrics`/`get_module_dependencies` gain a documented default `limit`; `search_symbols`/`dead_code_detection` get documented defaults (≈200/500/200/200 — calibrate so this repo's `src/` stays < ~200 KB). |
| Scope | Production 6: `src/python_refactor_mcp/models.py`, `tools/navigation/outline.py`, `server.py`, `tool_registry.py`, `tools/metrics/complexity.py`, `tools/metrics/dependencies.py`. Over target; coherence: one regression shape across the five tools the row names, each needing model field + implementation + MCP wrapper; search_symbols/dead_code add 0 files. Tests 4: `tests/unit/test_navigation_tools.py`, `tests/unit/test_metrics_tools.py`, `tests/contract/test_mcp_protocol.py` (advertised defaults), `tests/integration/test_end_to_end.py` (`:266-268` → `payload["items"]`). Docs 2: `docs/tool-reference.md`, `ai_docs/domains/python-refactor/reference.md`. Fragment `changelog.d/changed-breaking-bl-0010.md`. |
| Tool policy | edit-only |
| Estimated context cost | 60000 |
| Risks | BREAKING (operator OK'd 2026-09-24): outline list → object; default caps reduce item counts — PR-body callout, callers must check `truncated`. code_metrics order changes (observable). Do not cap the internal module-graph default (layer/cycle analysis consume it). Fanout: `get_symbol_outline` return used only at `server.py:175` (re-export in `tools/navigation/__init__.py` unchanged); new-field construction sites `complexity.py:102`, `dependencies.py:300`; ripple 6. Overlaps bl-0014 (outline.py), bl-0016 (server.py/tool_registry.py), bl-0011/0013/0014 (models.py) — serialized. |
| Validation | pyright on the 6 files; `pytest tests/unit/test_navigation_tools.py tests/unit/test_metrics_tools.py tests/contract/test_mcp_protocol.py -q`; repro: outline of `src/python_refactor_mcp` < ~200 KB, `truncated:true`, `returned_nodes<=250`; deps/metrics over `src/` report `truncated`. `just ci` with `CHANGELOG_BASE_REF=<baseRef>` (full run serialized per addenda). |
| Performance review | Cuts workspace outline transport 7.5 MB → <200 KB; node counting O(returned); metrics sort O(n log n), negligible vs AST parsing. |
| CHANGELOG category | Changed — BREAKING |
| CHANGELOG entry (draft) | - **Changed — BREAKING:** `get_symbol_outline` returns `{items, total_count, offset, truncated, total_nodes, returned_nodes}` and caps workspace scans at 250 nodes by default (`max_nodes`); `code_metrics`, `get_module_dependencies`, `search_symbols` and `dead_code_detection` apply documented default limits and report `truncated`. |
| Backlog sync | Close rows: [bl-0010]. Mark obsolete: []. |

### 9. bl-0011

| Field | Content |
|---|---|
| Route | direct |
| Diagnosis | `tools/metrics/architecture.py:84-95` `_get_layer` matches `pattern in parts` (single path/dotted component), so a dotted pattern `pkg.backends` never matches; `:117-120` keeps only the last alias of `import a, b`; `:121` skips relative imports (`node.module` None or level ignored). No report of patterns that match nothing. Confirms the row. |
| Approach | Acceptance (verbatim): (1) `layers=[["pkg.backends"],["pkg.tools"]]` reports the same violations as component patterns `[["backends"],["tools"]]`. (2) `import a, b` checks both names. (3) a layer pattern that matches no scanned module is reported, not silently ignored. Fix: derive the source file's dotted module name relative to the workspace root (and any `src/` layout), match a dotted pattern by dotted-prefix (`name == p or name.startswith(p + '.')`) and a plain pattern by component (existing behaviour); iterate every alias; resolve `ImportFrom` with `level > 0` against the source module's package; collect patterns never matched by a scanned source module and return them in a new additive `unmatched_layer_patterns: list[str]` field on `LayerViolationResult`. |
| Scope | Production (2): `src/python_refactor_mcp/tools/metrics/architecture.py`, `src/python_refactor_mcp/models.py` (additive field on `LayerViolationResult`). Tests (1): `tests/unit/test_metrics_tools.py`. |
| Tool policy | edit-only |
| Estimated context cost | 35000 |
| Risks | Additive output field — non-breaking; contract tests that snapshot output schemas may need updating (`tests/contract/`). `models.py` is shared with bl-0013 (RefactorResult) — serialize. fanoutEstimate: `LayerViolationResult(` constructed only in architecture.py — 0 extra. |
| Validation | Regression test first (red), then `just ci` with `CHANGELOG_BASE_REF=<baseRef>`; per-edit `.venv/Scripts/python.exe -m pytest <test file> -q` + pyright on changed files. Fragment `changelog.d/fixed-bl-0011.md`. |
| Performance review | N/A — correctness fix, no hot-path changes. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | - **Fixed:** `check_layer_violations` now matches dotted layer patterns, checks every alias of `import a, b`, resolves relative imports, and reports layer patterns that match no scanned module. |
| Backlog sync | Close rows: [bl-0011]. Mark obsolete: []. |

### 10. bl-0012

| Field | Content |
|---|---|
| Route | deepen |
| Diagnosis | `backends/rope_backend.py:98-101` `_build_inline_default` returns `ArgumentDefaultInliner(op.index)`; rope sets `self.remove = False` (`rope/refactor/change_signature.py:262-272`) and clears the default only when `remove` is true, so call sites are inlined but the def keeps `= 2`. The LibCST post-pass is not the cause (`tools/refactoring/signature_annotations.py:101-116` blocks the default; `_fill` `:143` only fills missing). `SignatureOperation.index` (`models.py:327`) is unconstrained and `server.py:210-221` passes it through; rope Python-indexes, so `-1` hits the last param. New hazard once removal works: `def f(a=1, b=2)` index 1 → `def f(a=1, b)` (SyntaxError). |
| Approach | (1) `_build_inline_default`: `inliner.remove = True`. (2) `RopeBackend.change_signature._work`: before `get_changes`, check each `inline_default` op against `ChangeSignature(project, resource, offset).get_args()` (`change_signature.py:79-86`, includes `self`); reject with bl-0002 `ToolInputError` naming `index`: out of range; target has no default; an earlier positional param still has a default. Full check only when `inline_default` is the first op; later chained ops get the range check, limit stated in the docstring. (3) `models.py:327`: `index: int \| None = Field(default=None, ge=0)` (mirror `Position.line` `:11`) — covers `change_signature`. (4) `server.py:210-221`: `index: Annotated[int, Field(ge=0)]` so SDK validation rejects `-1` with a visible error and the schema shows `minimum: 0`. |
| Scope | Production 4: `src/python_refactor_mcp/backends/rope_backend.py`, `models.py`, `server.py`, `backends/_threading.py` (`defect-forced-companion`: `rope_backend.py:168` raises `ToolInputError` inside `run_in_thread`; `_threading.py:50` `except Exception` rewraps it as `RopeError`; `tool_runtime.py:281` redacts it to caller_summary → the `[INVALID_INPUT]` message naming `index` never reaches the caller; fix: re-raise `ToolInputError` unwrapped). Tests 4 (extended): `tests/unit/test__threading.py` (ToolInputError propagates unwrapped), `tests/unit/test_rope_backend.py` (real rope, mirror `:30-47`: `def helper(a: int, b: int = 2)` + `helper(1)` idx 1 → `def helper(a: int, b: int)` / `helper(1, 2)`; rejects no-default, preceding-default, out-of-range), `tests/unit/test_models.py` (index=-1 → ValidationError), `tests/integration/test_end_to_end.py` (`index:-1` → `is_error` naming `index`). Fragment `changelog.d/changed-breaking-bl-0012.md`. |
| Tool policy | edit-only |
| Estimated context cost | 45000 |
| Risks | BREAKING (operator OK'd 2026-09-24): negative `index` rejected for every change_signature op, plus new no-default/preceding-default rejections — PR-body callout. dependsOn bl-0002 (`ToolInputError`). Fanout: `SignatureOperation` referenced in 6 prod files, constructed only at `server.py:204,220` (no negatives) → ripple 3. rope index 0 is `self` for methods — keep consistent. `new_order` negatives still unconstrained (follow-on). `docs/tool-reference.md:78` understates the tool (follow-on). Overlaps bl-0013 (rope_backend.py/models.py), bl-0010/0016 (server.py) — serialized. |
| Validation | pyright on the 3 files; `pytest tests/unit/test_rope_backend.py tests/unit/test_models.py tests/unit/test_signature_annotations.py -q`; repro preview on `def f(a, b=2)` idx 1 removes `= 2` and inlines `2`; `index:-1` errors. `just ci` with `CHANGELOG_BASE_REF=<baseRef>` (full run serialized per addenda). |
| Performance review | N/A — correctness fix, no hot-path changes. |
| CHANGELOG category | Changed — BREAKING |
| CHANGELOG entry (draft) | - **Changed — BREAKING:** `argument_default_inliner` / `change_signature` `inline_default` now removes the default from the signature as documented; a negative `index` is rejected for every `change_signature` operation, and inlining is rejected when the target has no default or an earlier parameter keeps one. |
| Backlog sync | Close rows: [bl-0012]. Mark obsolete: []. |

### 11. bl-0013

| Field | Content |
|---|---|
| Route | deepen |
| Diagnosis | `RefactorResult` (`models.py:273-281`) has only `edits` + `files_affected`. `RopeBackend._changes_to_edits` (`backends/rope_backend.py:250-269`) `continue`s past every non-`ChangeContents` change (`:254`), dropping rope `MoveResource`/`CreateFolder`/`CreateFile`/`RemoveResource` (rope `change.py:184,260,271,282`); `_build_result` (`:302-328`) derives everything from that. Producers: `module_to_package` (`:834-846`, rope `topackage.py:14-23`: identity ChangeContents + CreateFolder + MoveResource) and `move_module` (`:956-976`, importer edits + MoveResource → 0 edits with no importers); also `fix_module_names`, `generate_code` module/package. Apply arm (no change stack) uses `_apply_edits` (`:320`, `:279-300`) — TextEdits only — so by reading, `move_module(apply=True)` never moves the file; only `commit_change_stack` (`:1224`) / transactions (`:1508`) call `project.do`. |
| Approach | (1) `models.py`: `FileOperation(BaseModel)` beside `TextEdit` (`:36-41`): `kind: Literal["create_file","create_folder","move","remove"]`, `path: str`, `new_path: str \| None = None`; `RefactorResult.file_operations: list[FileOperation] = Field(default_factory=list)`. (2) `rope_backend.py`: `_changes_to_file_operations` beside `_changes_to_edits`, flattening nested ChangeSets (as rope `contrib/changestack.py:45-50`); skip identity ChangeContents in `_changes_to_edits`; populate `file_operations` and fold op paths into `files_affected` in `_build_result` and `commit_change_stack` (`:1231-1233`); when a change set has resource ops and no change stack is active, apply via `project.do(changes)` with the originals/rollback shape of `commit_change_stack` (`:1214-1230`). Test-first: if a `move_module(apply=True)` test already passes on current code, drop the apply-arm change. |
| Scope | Production 2: `src/python_refactor_mcp/models.py`, `src/python_refactor_mcp/backends/rope_backend.py`. Tests 2: `tests/unit/test_rope_backend.py` (real rope: module_to_package preview lists folder create + move; move_module with no importers lists the move; apply moves the file), `tests/unit/test_models.py`. Fragment `changelog.d/fixed-bl-0013.md`. Within target. |
| Tool policy | edit-only |
| Estimated context cost | 40000 |
| Risks | Fanout: 34 `RefactorResult(` constructions in 17 files; new field is optional with default and no contract snapshot asserts exact `model_dump` → ripple 2. Apply arm is the same root cause (dropped resource changes) — kept test-first. `attach_post_apply_diagnostics` (`util/shared.py`) must tolerate the moved-away old path; the moved module's TextEdit targets the pre-move path. `split_module` has no file-op pattern to mirror. Overlaps bl-0006/bl-0012 (rope_backend.py), bl-0010/0011/0014 (models.py) — serialized. |
| Validation | pyright on both files; `pytest tests/unit/test_rope_backend.py tests/unit/test_models.py -q`; manual preview/apply repro in a tmp workspace. `just ci` with `CHANGELOG_BASE_REF=<baseRef>` (full run serialized per addenda). |
| Performance review | N/A — correctness fix, no hot-path changes. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | - **Fixed:** `move_module`, `module_to_package` and other rope file-moving refactors now list file moves/creations in a new `RefactorResult.file_operations` field in preview, and `apply=True` performs those moves instead of writing only the content edits. |
| Backlog sync | Close rows: [bl-0013]. Mark obsolete: []. |

### 12. bl-0014

| Field | Content |
|---|---|
| Route | direct |
| Diagnosis | `tools/search/_helpers.py:122-142` `resolve_target_files` returns `python_files(<missing root>)` = `[]` or a nonexistent resolved file, so `dead_code_detection`/`unused_symbol_sweep` report `{items:[],scan_failures:[]}`; `tools/navigation/outline.py:164-166` `_fetch` returns `[]` for a non-file; `tools/metrics/coverage.py:69-70` headlines `100.0` when `total_functions == 0` even if every path failed. |
| Approach | Acceptance (verbatim): (1) `dead_code_detection{root_path:<missing>}` returns a `scan_failures` entry. (2) `get_symbol_outline{file_path:<missing>}` errors or reports the failure. (3) `get_type_coverage` on a missing file does not headline `100.0`. Fix: `resolve_target_files` returns targets plus `ScanFailure(phase="resolve", error_type="FileNotFoundError")` entries for a missing `root_path`/`file_path`/`file_paths` member; its two callers merge them into their `scan_failures`. `get_symbol_outline` raises `ToolInputError` (the caller-safe class bl-0002 adds to `errors.py`) for an explicit missing `file_path`/`file_paths` member or `root_path`. `get_type_coverage` reports percentages only over scanned files: when `files_scanned == 0` the pct fields are `None` (make them `float | None` in `TypeCoverageReport`). |
| Scope | Production (6): `src/python_refactor_mcp/tools/search/_helpers.py`, `tools/search/dead_code.py`, `tools/search/unused_symbols.py`, `tools/navigation/outline.py`, `tools/metrics/coverage.py`, `models.py`. Over the 4-file target: one regression shape (missing scope must not read as clean) across the three tool families the Acceptance names; dead_code.py/unused_symbols.py are the only two callers of `resolve_target_files` (`dead_code.py:103`, `unused_symbols.py:83`) and must consume its new failure list or the missing-root failure is still dropped (`defect-forced-companion`: `_helpers.py:137` (`python_files(effective_root)` on a missing root) → caller `scan_failures` assembly). Three mechanisms (resolve failures, outline raise, coverage pct) share one regression shape — a missing scope must never read as clean — and one fixture (a nonexistent path). Tests (3): `tests/unit/test_search_tools.py`, `tests/unit/test_navigation_tools.py`, `tests/unit/test_metrics_tools.py`. |
| Tool policy | edit-only |
| Estimated context cost | 58000 |
| Risks | `TypeCoverageReport` pct nullability is an output-schema change and the outline now raises where it returned `[]` — BREAKING (operator OK'd 2026-09-24), `changed-breaking-bl-0014.md` + PR-body callout. The outline FileNotFoundError reaches callers readably only after bl-0002 (caller-safe boundary) → dependsOn bl-0002. Shares `dead_code.py` with bl-0001, `outline.py` with bl-0010, `models.py` with bl-0011/bl-0013. Fanout probe: `rg resolve_target_files src` → 2 callers (dead_code.py:103, unused_symbols.py:83), both in Scope; fanoutEstimate 6. |
| Validation | Regression test first (red), then `just ci` with `CHANGELOG_BASE_REF=<baseRef>`; per-edit `.venv/Scripts/python.exe -m pytest <test file> -q` + pyright on changed files. Fragment `changelog.d/changed-breaking-bl-0014.md`. |
| Performance review | N/A — correctness fix, no hot-path changes. |
| CHANGELOG category | Changed — BREAKING |
| CHANGELOG entry (draft) | - **Changed — BREAKING:** a nonexistent `root_path`/`file_path` no longer reads as a clean result — dead-code/unused sweeps report a `scan_failures` entry, `get_symbol_outline` errors, and `get_type_coverage` no longer headlines 100%. |
| Backlog sync | Close rows: [bl-0014]. Mark obsolete: []. |

### 13. bl-0015

| Field | Content |
|---|---|
| Route | deepen |
| Diagnosis | Against upstream `modelcontextprotocol/mcpb` `schemas/mcpb-manifest-v0.3.schema.json` (draft-07, `additionalProperties: false`, required `name, version, description, author, server`): `manifest.json:7` `author` is a string (needs `{name,email?,url?}`); `:9` `repository` string (needs `{type,url}`); `:13` `categories` not a v0.3 property; `:15-19` `tools` holds launch config instead of `[{name, description?}]`; `server` missing (v0.3 `server.type` ∈ python/node/binary). No `compatibility.runtimes.python` despite `pyproject.toml:10` `>=3.14`; env vars `PYTHON_REFACTOR_MCP_TOOL_PROFILE` (`config.py:15`) and `MAX_WORKSPACES` (`config.py:37`) undeclared. Nothing validates the file; `scripts/bump_reinstall.py:92-107,125` touches only `version`. |
| Approach | (1) Rewrite `manifest.json` to v0.3: keep `manifest_version`, `name`, `display_name`, a single top-level `version` line (the `bump_reinstall.py:125` regex must match exactly once), `description`, `license`, `privacy_policies`, `icon`; `author` `{name: darylmcd, url}`; `repository` `{type: git, url}`; `homepage`; `categories` → `keywords`; `server: {type: python, entry_point: src/python_refactor_mcp/__main__.py, mcp_config: {command: python, args: [-m, python_refactor_mcp], env: {PYTHON_REFACTOR_MCP_TOOL_PROFILE: ${user_config.tool_profile}, MAX_WORKSPACES: ${user_config.max_workspaces}}}}`; `user_config.tool_profile` (string, default refactoring, not required), `user_config.max_workspaces` (number, default 3, min 1); `compatibility: {platforms: [win32, darwin, linux], runtimes: {python: ">=3.14"}}`; `tools_generated: true` (surface is profile-dependent). (2) Vendor the upstream v0.3 schema byte-for-byte (re-fetch; cite URL + date in the test docstring) at `tests/unit/data/mcpb-manifest-v0.3.schema.json`. (3) `tests/unit/test_manifest.py`: `jsonschema.Draft7Validator` finds no errors; `version` == pyproject version; every `${user_config.X}` resolves; bump round-trip on a tmp copy via `bump_reinstall.update_version_surfaces`/`read_release_version` (mirror `tests/unit/test_bump_reinstall.py:21-31`) then re-validate. Runs inside `just ci` + hosted CI via the existing `test` recipe — no justfile/ci.yml edit. (4) `pyproject.toml` dev extra `jsonschema>=4.26` (today transitive via `mcp`) + `uv lock`. (5) `ai_docs/runtime.md:73` manifest row. |
| Scope | Production 3: `manifest.json`, `pyproject.toml`, `uv.lock` (lock refresh forced by `uv sync --locked`). Tests 2 new: `tests/unit/test_manifest.py`, `tests/unit/data/mcpb-manifest-v0.3.schema.json`. Doc 1: `ai_docs/runtime.md`. Deliberate deviation from the detail anchors: no `scripts/validate_manifest.py` / justfile recipe — the pytest check already runs in `just ci` (Directive #6); `bump_reinstall.py` unchanged, covered by the round-trip test. Fragment `changelog.d/fixed-bl-0015.md`. |
| Tool policy | edit-only |
| Estimated context cost | 35000 |
| Risks | No nested key may be named `version` (bump regex) — the round-trip test locks it. Clients reading `mcp_config` literally get unsubstituted `${user_config.*}` (local registration lives in `~/.claude.json`, no impact); confirm `config.py:27` rejects a bad profile loudly. `tools_generated` over a drifting static list is a judgment call. `format: email/uri` not enforced without extra deps. Fix toward a published spec → `fixed-*`. Fanout: prod readers of the manifest are `bump_reinstall.py` (version only) and `changelog_fragments.py:41` (filename only) → ripple 3. Pin v0.3 to match `manifest_version`. |
| Validation | `pytest tests/unit/test_manifest.py tests/unit/test_bump_reinstall.py -q`; negative check: string `author` fails with path `author`; `uv lock --check`. `just ci` with `CHANGELOG_BASE_REF=<baseRef>` (full run serialized per addenda). |
| Performance review | N/A — correctness fix, no hot-path changes. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | - **Fixed:** `manifest.json` now conforms to the MCPB v0.3 schema (object `author`/`repository`, a `server` launch block, declared Python >=3.14 runtime and tool-profile/workspace env config, `tools_generated` instead of misplaced launch config), and `just ci` validates it against the vendored schema. |
| Backlog sync | Close rows: [bl-0015]. Mark obsolete: []. |

### 14. bl-0016

| Field | Content |
|---|---|
| Route | deepen |
| Diagnosis | `server.py:73-92` builds one static `_SERVER_INSTRUCTIONS` for both profiles (only the profile name interpolated at `:76`); examples `:80-84` and tips `:87-91` name tools regardless of profile; `:83` claims all refactoring tools preview. `register_tools` (`tool_registry.py:1782-1788`) advertises raw docstrings, so `Related:` hints cite off-profile tools (membership: `_profile_includes` `:1736-1740` + `_REFACTORING_SUPPORT_TOOLS` `:1606-1632`). Live probe at `fecd44c`: 4 off-profile names in instructions, 19 descriptions — only `prepare_rename` (`:934`) and `apply_type_annotations` (`:658`) outside `Related:`. No-preview wording missing on `restart_server` `:1510`, `undo_refactoring` `:1518`, `redo_refactoring` `:1526`, `commit_change_stack` `:1550`, `refactor_transaction` `:1443`. Nits: `list_environments` `:1490` cites `get_context`; `project_search` `:1502` "workspace/symbol"; `structural_search` `:1196-1205` undocumented `language`. |
| Approach | (1) `tool_registry.py`: pure `profile_description(doc, advertised: frozenset[str]) -> str` — filter the `Related:` clause (to next `. ` or end) to advertised identifiers, keep parentheticals, drop an empty clause, preserve trailing text (positions sentence); `register_tools` passes `description=profile_description(inspect.getdoc(record.func) or "", selected_names)` to `add_tool`. (2) Hand edits: `prepare_rename` body "before renaming"; `apply_type_annotations` body drops named read tools; append "Acts immediately — no preview; there is no apply parameter." to the 5 tools (transaction: commits on success); nits → `list_environments` Related `server_status`, `project_search` cites `search_symbols`, `structural_search` documents `language` (python only). `create_type_stubs` wording owned by bl-0007. (3) `server.py`: `build_server_instructions(profile, advertised)` of `(text, required_names)` entries emitted only when all required names are advertised; analysis profile notes refactoring needs the `refactoring` profile; preview line → "Refactoring tools that accept apply default to preview (apply=False); tools without an apply parameter act immediately." Build `MCPServer` after `EXPLICIT_TOOL_RECORDS` (`mcp` used only at `:94,372,393`). |
| Scope | Production 2: `src/python_refactor_mcp/server.py`, `src/python_refactor_mcp/tool_registry.py` — `tool-surface-only`. Tests 2: `tests/contract/test_mcp_protocol.py` (per-profile: advertised descriptions + instructions cite no off-profile tool; immediate-write tools say "no preview"; no "All refactoring tools default to preview"; explicit empty allowlist for prose false positives), `tests/unit/test_server.py` (`profile_description` pruning/empty/parenthetical/trailing sentence; keep `:127`). Fragment `changelog.d/fixed-bl-0016.md`. |
| Tool policy | edit-only |
| Estimated context cost | 40000 |
| Risks | dependsOn bl-0007 (owns `create_type_stubs` "no preview" wording; if bl-0007 is deferred, add it here and drop the dep). `add_tool(description=)` replaces docstring text only (`mcp/server/mcpserver/server.py:603-613`); `test_position_based_tools_document_zero_based_convention` must stay green. When bl-0020 lands, parameter descriptions must survive the filter. Hotspot overlap bl-0010/bl-0012 (server.py/tool_registry.py) — serialized. Fanout: all edits in 2 files → ripple 2. |
| Validation | `pytest tests/contract/test_mcp_protocol.py tests/unit/test_server.py -q`; pyright on both files; re-run the per-profile scan → 0 off-profile names; start with `PYTHON_REFACTOR_MCP_TOOL_PROFILE=analysis` and confirm instructions name no refactoring tools. `just ci` with `CHANGELOG_BASE_REF=<baseRef>` (full run serialized per addenda). |
| Performance review | N/A — one string filter per tool at startup. |
| CHANGELOG category | Fixed |
| CHANGELOG entry (draft) | - **Fixed:** Server instructions and tool `Related:` hints are profile-aware, so they never cite tools the active profile does not advertise; tools that act immediately (undo/redo, commit, transaction, restart) now say they skip preview. |
| Backlog sync | Close rows: [bl-0016]. Mark obsolete: []. |
