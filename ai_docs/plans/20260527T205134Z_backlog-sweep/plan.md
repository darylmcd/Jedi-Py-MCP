# Backlog sweep plan — 20260527T205134Z

**Generated:** 2026-05-27T20:51:34Z
**Backlog snapshot:** 2026-05-27T05:00:00Z
**Selected:** 6 initiatives (count=6, scope=all)
**Addenda:** none (no `ai_docs/prompts/backlog-sweep-addenda.md`)

Selection rationale: top-of-backlog by correctness risk → blast radius → feasibility. Two High (refactor heavy duplication), one Medium (refactor LSP helper), three Low (foundational new tools whose prerequisites are now satisfied). No Rule 1 bundle candidates — all six touch disjoint primary anchors.

---

### 1. server-tool-registration-table

| Field | Content |
|---|---|
| Status | merged (PR #52, 2026-05-28) |
| Backlog rows closed | server-tool-registration-table |
| Diagnosis | Confirmed in `src/python_refactor_mcp/server.py` (1815 lines as of 2026-05-27; backlog cited 1574 — count has grown since the audit but the core problem is identical). `grep -c '^@mcp.tool'` returns 91 registrations. Of these, 83 follow a pure 5-line delegation pattern: `@mcp.tool(annotations=<CONST>) / @_tool_error_boundary / async def <name>(ctx, ...) -> T: / app = _get_current_backends() / result = await <module>.<name>(app.<backend>, ...) / await ctx.debug(f"...") / return result`. The remaining ~8 wrappers embed extra inline logic (`get_completions` lines 407-413: fuzzy-branch dispatch; `get_inlay_hints` lines 470-475: file line-count read; `security_scan` line 1794: `_ = _get_current_backends()` no-op). The `_tool_error_boundary` decorator and `_get_current_backends()` helper are entirely private to `server.py` — no callers outside the file were found. The `mcp` instance and `run_server` function are the only public exports; their signatures are unchanged by this refactor. |
| Approach | 1. Create `src/python_refactor_mcp/tool_registry.py` — define a `ToolRecord` datatype (or TypedDict) holding `(name, annotations, callable, debug_fmt_fn)` and a `register_tools(mcp_instance)` function that iterates the table, applies `_tool_error_boundary`, and calls `mcp_instance.tool(annotations=...)` for each record. The `debug_fmt_fn` is a small callable `(result) -> str` that produces the `ctx.debug` message. 2. Rewrite `src/python_refactor_mcp/server.py` — remove all 91 per-tool `async def` functions and replace with a call to `register_tools(mcp)` after the `_tool_error_boundary` definition. The ~8 non-trivial wrappers that have inline conditional logic (e.g. `get_completions` fuzzy branch, `get_inlay_hints` line-count fallback) should remain as explicit named async functions in `server.py`, decorated normally, rather than being force-fit into the table — this avoids obscuring their non-trivial logic. Target residual for `server.py` is ~400-450 lines (infrastructure + ~8 explicit non-trivial wrappers + `run_server`); `tool_registry.py` carries the 83-entry table (~500-600 lines). 3. Update `tests/unit/test_server.py` line 14 — the hardcoded `len(tools) == 91` assertion remains correct; just verify the import path still resolves (no change needed unless the `mcp` object moves). No other test files reference the wrapper function names directly. |
| Scope | Production files touched: 2 — `src/python_refactor_mcp/server.py` (major rewrite), `src/python_refactor_mcp/tool_registry.py` (new file). Test files modified: 1 — `tests/unit/test_server.py` (verify assertion still passes; may need a comment update if line numbers shift). Satisfies Rule 3 (≤ 4 production files) and Rule 4 (≤ 3 test files). No Rule 3 exemption needed — 2 production files is within the standard fix/refactor cap. |
| Tool policy | edit-only |
| Estimated context cost | 55000 |
| Risks | (1) The ~8 non-trivial wrappers must be identified exhaustively before deletion — missing one will silently drop logic. Executor should grep for any wrapper body containing more than one `await` call or any conditional before deleting. (2) `_tool_error_boundary` wraps functions via `@wraps(func)` which copies `__name__`; the registration table approach must preserve the tool name passed to FastMCP — confirm FastMCP uses the decorated function name, not the wrapper name. (3) FastMCP's `tool()` decorator relies on function introspection for parameter schema generation; wrapping via a factory instead of a named `async def` may change schema output. The executor must run `pytest tests/unit/test_server.py` (which calls `await server.mcp.list_tools()` and asserts 91 tools with correct schema shape) immediately after the rewrite to catch any schema regression. (4) `test_doc_tool_count_drift.py` threshold of 50 is well below the tool count and polices doc files only — no risk from this test. (5) The `test_server.py` hardcoded `== 91` count is load-bearing; if tool count changes during the refactor (e.g. accidentally double-registering via both table and residual named functions) the test will catch it. (6) Fanout probe skipped beyond server.py — surgical in-file consolidation, no cross-cutting symbol changes to external callers. |
| Validation | 1. `pytest tests/unit/test_server.py` — must pass `test_server_registers_all_stage_one_tools` (91 tools, no `ctx` in schema). 2. `pytest tests/unit/test_doc_tool_count_drift.py` — must pass unchanged. 3. `pytest tests/` — full suite green. 4. Manual smoke: `python -m python_refactor_mcp --help` exits cleanly; `python -c "from python_refactor_mcp.server import mcp, run_server; print('ok')"` imports without error. 5. Line count check: `wc -l src/python_refactor_mcp/server.py` should be ≤ 500 post-refactor (current 1815). |
| Performance review | N/A — correctness fix, no hot-path changes. |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | Refactor: replace 83 identical per-tool wrapper functions in `server.py` with a declarative registration table in `tool_registry.py`; reduces `server.py` from 1815 lines to ~450 lines. No behavior changes. |
| Backlog sync | Close rows: [server-tool-registration-table]. |

<details>
<summary>Sonnet handoff notes</summary>

**Sonnet handoff:**

- **Pattern coordinates (canonical 5-line wrapper to collapse into table rows):** `src/python_refactor_mcp/server.py:330-354` (`find_references`) and `:386-393` (`get_type_info`) — these are the pure shape: `app = _get_current_backends()` → `await <module>.<name>(app.<backend>, ...)` → `await ctx.debug(f"...")` → `return result`. The `debug_fmt_fn` field on `ToolRecord` must produce strings of this exact form (per-tool `f"..."` formatting reads result attributes, e.g. `result.source`, `result.total_count`).
- **Non-trivial wrappers — keep as explicit `async def` in `server.py`, do NOT force into table:** `get_completions` at `server.py:398-414` (fuzzy branch dispatches to `app.jedi` vs `analysis`); `get_inlay_hints` at `:460-485` (reads file line count with `FileNotFoundError`/`OSError` handling); `security_scan` at `:1788-1797` (uses `_ = _get_current_backends()` no-op + imported alias `_security_scan`); `get_test_coverage_map` at `:1786` area (uses imported alias `_get_test_coverage_map`). Also keep any wrapper whose body has >1 `await` or a conditional — grep `^async def` returns 93 hits; with 91 registered tools, expect ~8 to remain explicit.
- **Registration call site:** the `mcp` instance is constructed at `:318-322` — registration must execute at import time after `mcp` exists. Insert `register_tools(mcp)` immediately after construction at `:322` (before the first `@mcp.tool` block at the original `:330`).
- **Test target:** `tests/unit/test_server.py:14` — keep `== 91` unchanged. Re-run after refactor; if the count drifts, the table is missing entries or double-registering. Do NOT edit the assertion to make it pass.
- **Edge cases to cover in `ToolRecord` / `register_tools`:** (1) FastMCP derives tool *name* and *schema* from the wrapped function's `__name__` and signature introspection — `_tool_error_boundary` already uses `@wraps(func)`, so preserve that; the table entry's callable must be the *undecorated* delegate, and `register_tools` applies `_tool_error_boundary` then `mcp.tool(annotations=...)` in that order (mirror `:330-332` decorator stacking). (2) Each delegate's signature must match the public tool surface exactly (param names, types, defaults) — FastMCP exposes these in `inputSchema`; the `test_server_registers_all_stage_one_tools` test at `:15` asserts `"ctx" not in inputSchema.properties`, so the synthesized wrapper must accept `ctx: MCPContext` as first param and have it stripped by `_tool_error_boundary`'s existing pathway. (3) Annotation constants (`_READONLY`/`_DESTRUCTIVE`/`_ADDITIVE` at `:77-79`) must be passed through unchanged per-tool — do not collapse to a single default.
- **Negative space:** do NOT modify `_tool_error_boundary` (`:180-272`), `_get_current_backends`, `app_lifespan` (`:278-297`), `_SERVER_INSTRUCTIONS` (`:300`), `mcp` construction (`:318-322`), or `run_server` (`:1805-1815`) — those are public-surface infrastructure and out of scope. Do NOT touch the `from python_refactor_mcp.tools.metrics.security import security_scan as _security_scan` aliasing at `:72-73` — the non-trivial wrappers keep these. Do NOT update `tests/unit/test_doc_tool_count_drift.py` (threshold is doc-file-only, well below 91).

</details>

### 2. backend-threaded-decorator

| Field | Content |
|---|---|
| Status | merged (PR #51, 2026-05-28) |
| Backlog rows closed | backend-threaded-decorator |
| Diagnosis | Confirmed: `src/python_refactor_mcp/backends/rope_backend.py` has exactly 30 `asyncio.wait_for(asyncio.to_thread(_work), timeout=self._timeout)` call sites (lines ~303–309, ~333–338, etc.) and `src/python_refactor_mcp/backends/jedi_backend.py` has exactly 17 (lines ~178–183, ~198–203, etc.). Every async method in both backends follows the identical shell: define `def _work()` closure → `await asyncio.wait_for(asyncio.to_thread(_work), timeout=self._timeout)` (rope additionally wraps this in `async with timed(...)`) → `except Exception as exc: raise <Backend>Error(...) from exc`. The only per-method variation is: (a) the `_work()` body itself, (b) the error message string, and (c) whether a `timed` context manager is used. Both error classes (`RopeError`, `JediError`) share the `BackendError` base in `src/python_refactor_mcp/errors.py:4-17`. No existing `_threading.py` module is present under `src/python_refactor_mcp/backends/`. |
| Approach | 1. Create `src/python_refactor_mcp/backends/_threading.py`. Define `async def run_in_thread(fn: Callable[[], T], timeout: float, error_cls: type[BackendError], op_name: str, logger: logging.Logger) -> T` that owns the `asyncio.wait_for(asyncio.to_thread(fn), timeout=timeout)` call, the `async with timed(logger, op_name)` wrapper (optional via sentinel/None), and the `except Exception as exc: raise error_cls(op_name + ": " + str(exc)) from exc` re-raise. The helper is a plain `async def`; no class is needed. 2. Rewrite all 30 rope async methods in `src/python_refactor_mcp/backends/rope_backend.py` — each method body becomes: define `def _work()`, then `return await _run_in_thread(_work, self._timeout, RopeError, "rope.<method>", _LOGGER)`. The `timed` context manager is absorbed inside `run_in_thread` (always active for rope; a `logger=None` shortcut can skip it for jedi where `timed` was absent). 3. Rewrite all 17 jedi async methods in `src/python_refactor_mcp/backends/jedi_backend.py` identically, passing `error_cls=JediError` and `logger=None` (no timed wrapping). Import `run_in_thread` from `._threading` in both backends. 4. The `timed` import in `rope_backend.py` and its per-method `async with` blocks are removed; `timed` is imported only inside `_threading.py`. |
| Scope | Production files touched: 3 — `src/python_refactor_mcp/backends/_threading.py` (new), `src/python_refactor_mcp/backends/rope_backend.py` (modified), `src/python_refactor_mcp/backends/jedi_backend.py` (modified). Test files added: 1 — `tests/unit/test__threading.py` (unit tests for `run_in_thread`: timeout propagation, correct error-class re-raise, timed-logger path, non-timed path). Rule 3 satisfied: 3 production files ≤ 4 cap; no exemption needed. Rule 4 satisfied: 1 test file ≤ 3 cap. |
| Tool policy | edit-only |
| Estimated context cost | 50000 |
| Risks | (1) Rope's `timed` context manager wraps the `wait_for` call itself — the `run_in_thread` helper must preserve the timing scope around the await, not around the closure definition. Get the nesting order wrong and the elapsed time reflects thread-scheduling overhead only. (2) Some rope methods have a bare `return result` inside the `timed` block after `wait_for`; others have a `_LOGGER.debug(...)` call before the return — these per-method debug logs must be preserved in the `_work()` body or after the `await run_in_thread(...)` return, not silently dropped. (3) The jedi backend does not use `timed`; verify that `run_in_thread` with `logger=None` does not accidentally import or call `timed`. (4) `asyncio.TimeoutError` is a subclass of `Exception` — the current `except Exception` in both backends catches timeouts and re-raises as `<Backend>Error`. The helper must preserve this behaviour (not filter `TimeoutError` out). (5) Adjacent behavior: `begin_change_stack`, `commit_change_stack`, `rollback_change_stack` in rope_backend are thin delegation methods that do NOT use the `_work/wait_for` shell — verify they are left untouched. |
| Validation | (1) `python -m pytest tests/unit/test__threading.py -v` — new unit tests cover: timeout raises `RopeError`/`JediError` (not raw `asyncio.TimeoutError`), inner exception is chained correctly via `__cause__`, timed-logger variant emits debug lines at start and end. (2) `python -m pytest tests/unit/test_rope_backend.py tests/unit/test_jedi_backend.py -v` — existing tests must pass unchanged; no method signature changes. (3) `python -m pytest tests/ -v` — full suite green. (4) Manual spot-check: count `asyncio.wait_for` occurrences in both backend files post-refactor — expected 0. (5) `grep -n "async with timed" src/python_refactor_mcp/backends/rope_backend.py` — expected 0 occurrences (all moved into `_threading.py`). (6) Build / import check: `python -c "from python_refactor_mcp.backends.rope_backend import RopeBackend; from python_refactor_mcp.backends.jedi_backend import JediBackend"` — no import errors. |
| Performance review | N/A — correctness fix, no hot-path changes. The helper is a thin await wrapper; no additional allocation or scheduling overhead beyond the current pattern. |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | Extracted shared `run_in_thread` async helper into `backends/_threading.py`, eliminating 47 duplicate `asyncio.wait_for / to_thread / except` boilerplate blocks across `RopeBackend` and `JediBackend`. No behaviour change. |
| Backlog sync | Close rows: [backend-threaded-decorator]. |

<details>
<summary>Sonnet handoff notes</summary>

**Sonnet handoff:**

- **Pattern coordinates:** the canonical rope shape is `src/python_refactor_mcp/backends/rope_backend.py:295-309` (`rename` — `_work` closure + `async with timed(...)` + `wait_for(to_thread(...))` + post-await `_LOGGER.debug` + `except Exception → RopeError(...) from exc`). The canonical jedi shape is `src/python_refactor_mcp/backends/jedi_backend.py:165-183` (`get_references` — `_work` + `wait_for(to_thread(...))` + post-await `_LOGGER.debug` + `except Exception → JediError(...) from exc`, no `timed`). `timed` is defined at `src/python_refactor_mcp/util/timing.py:11-26`; `BackendError` at `src/python_refactor_mcp/errors.py:4-17`.
- **Helper signature:** `async def run_in_thread(fn, *, timeout, error_cls, op_name, logger=None) -> T`. When `logger` is non-None wrap the await in `async with timed(logger, op_name):`; when None skip it (do not import `timed` conditionally — import at module top, just gate the `async with`). Re-raise as `error_cls(f"{op_name} failed: {exc}") from exc`. Do NOT filter `asyncio.TimeoutError` — current `except Exception` catches it and the helper must too.
- **Per-method debug preservation:** several rope methods log post-await (e.g. `rope_backend.py:306` `"rope rename produced %d edits"`). These are method-specific format strings — leave them in the caller after `await run_in_thread(...)`, not absorbed into the helper.
- **Test target:** add `tests/unit/test__threading.py` (new file, sibling of existing `tests/unit/test_rope_backend.py`). Cover: (a) `TimeoutError` re-raises as `RopeError`/`JediError` with `__cause__` chain; (b) generic exception path same; (c) `logger=None` skips `timed` (no debug lines); (d) `logger=_LOGGER` emits start+completed debug lines via `caplog`.
- **Negative space:** do NOT touch `rope_backend.py:994` `begin_change_stack`, `:1003` `commit_change_stack`, `:1014` `rollback_change_stack` — they are thin delegations without the `_work`/`wait_for` shell. Do NOT modify `src/python_refactor_mcp/backends/pyright_lsp.py` — that's the LSP backend, separate refactor (initiative 3).

</details>

### 3. pyright-lsp-position-request-helper

| Field | Content |
|---|---|
| Status | merged (PR #50, 2026-05-28) |
| Backlog rows closed | pyright-lsp-position-request-helper |
| Diagnosis | `src/python_refactor_mcp/backends/pyright_lsp.py` (1427 lines) confirmed as the sole anchor — file exists and the described pattern is present. Exact shape: 11 methods with signature `(self, file_path: str, line: int, char: int)` (lines 511, 570, 617, 659, 685, 715, 847, 915, 1077, 1105, 1133, 1163) each execute the verbatim sequence `absolute_path = normalize_path(file_path)` → `await self.ensure_file_open(absolute_path)` → `await self._request(method, {"textDocument": {"uri": path_to_uri(absolute_path)}, "position": {"line": line, "character": char}, ...})` → `if "error" in response: [optional is_unhandled_method_error branch] raise PyrightError(...)` → result-shape dispatch. The backlog's "~40 methods" is an overcount: 20 methods use the normalize+ensure+request triple, but only 11 have the canonical textDocument+position envelope. The remaining 9 (e.g. `get_document_symbols`, `get_folding_ranges`, `get_semantic_tokens`) use file-only params and do not fit the helper's target signature. The helper targets the 11 position methods only. |
| Approach | 1. Add private async helper `_position_request(self, lsp_method: str, absolute_path: str, line: int, char: int, extra_params: dict \| None = None) -> JSONDict` in `src/python_refactor_mcp/backends/pyright_lsp.py` immediately before the Analysis features section (~line 509). The helper owns: `await self.ensure_file_open(absolute_path)`, build `{"textDocument": {"uri": path_to_uri(absolute_path)}, "position": {"line": line, "character": char}, **extra_params}`, `await self._request(lsp_method, params)`, return the raw response dict. Callers then only check `"error"` and dispatch the result. 2. Rewrite each of the 11 position methods to: (a) keep `absolute_path = normalize_path(file_path)` as the first line, (b) call `response = await self._position_request(lsp_method, absolute_path, line, char, extra_params)` in place of the ensure+request block. Methods with an `extra_params` argument (`get_references` adds `"context": {...}`) pass that dict. Each method retains its own error-check and result-dispatch logic — those are NOT pulled into the helper because result shapes differ widely (`definition_entry_to_locations`, model construction, `None` short-circuits, `is_unhandled_method_error` branches). Mirror the `ensure_file_open` + `path_to_uri` call pattern already used at lines 511–522 (`get_hover`). 3. No changes to public method signatures, return types, or docstrings. |
| Scope | Production files: 1 (`src/python_refactor_mcp/backends/pyright_lsp.py`). Test files: 1 (extend `tests/unit/test_pyright_lsp.py` with a parameterized test asserting that `_position_request` calls `ensure_file_open` + `_request` with the correct textDocument+position params, and that caller methods pass through the result unchanged). No files created or deleted. Rule 3 satisfied: 1 production file, 1 test file. |
| Tool policy | edit-only |
| Estimated context cost | 45000 |
| Risks | 1. The 11 position methods are not entirely uniform — `get_references` appends a `"context"` field, `prepare_call_hierarchy` is multi-line signature, `get_references` has `include_declaration` param. Executor must ensure `extra_params` merge does not clobber `textDocument` or `position` keys. 2. `prepare_rename` (line 1163) has heavier post-processing that reads the file for a placeholder; its error-check has an `is_unhandled_method_error` branch. Verify result dispatching is not accidentally shortened. 3. `get_completions` (line 570) extracts `result.get("items")` from a nested dict — it follows the pattern but result dispatch is uniquely branching; confirm it is included and correct. 4. Fanout probe: `_position_request` is a new private symbol (no existing callers to grep). The 11 target methods all live inside the class; public method signatures are unchanged, so callers in `tests/` and `src/python_refactor_mcp/` do not need updating. `fanoutEstimate: 1` (single file). |
| Validation | 1. `pytest tests/unit/test_pyright_lsp.py -v` — existing tests must all pass. 2. New parameterized test in `tests/unit/test_pyright_lsp.py` covering `_position_request`: mock `FakeLSPClient`, assert `textDocument/definition`, `textDocument/hover`, and at least one `extra_params` case (`textDocument/references` with context) all produce the correct request payload. 3. `python -m mypy src/python_refactor_mcp/backends/pyright_lsp.py` (or `pyright src/`) — no new type errors. 4. Spot-check `get_declaration` (line 1077) which falls back to `get_definition` on `is_unhandled_method_error` — this fallback is in the method body (caller of `_position_request`), not in the helper itself; confirm the fallback still fires correctly after refactor. |
| Performance review | N/A — correctness fix, no hot-path changes. The helper adds one async call frame but eliminates no I/O; the actual LSP round-trip cost dominates. |
| CHANGELOG category | Maintenance |
| CHANGELOG entry (draft) | Refactor `PyrightLSPClient`: extract `_position_request` helper to deduplicate the normalize/open/request/error boilerplate shared across 11 `textDocument/*` position methods, reducing `pyright_lsp.py` by ~110 lines. No behavior change. |
| Backlog sync | Close rows: [pyright-lsp-position-request-helper]. |

<details>
<summary>Sonnet handoff notes</summary>

**Sonnet handoff:**

- **Pattern coordinates:** mirror `src/python_refactor_mcp/backends/pyright_lsp.py:511-540` (`get_hover`) for the canonical normalize → `ensure_file_open` → build `textDocument`+`position` params → `_request` → `if "error" in response` → result dispatch sequence. The new `_position_request` helper owns lines equivalent to `:514` (ensure) + `:516-522` (request build/call) and returns the raw response dict; callers retain their own `:523-524` error-check and `:526+` result dispatch.
- **Helper insertion point:** add `_position_request` immediately before line 511 (right after the `# ── Analysis features ──` banner at `:509`). Use `_request` (defined at `:184`) for the underlying call. Param construction: `params = {"textDocument": {"uri": path_to_uri(absolute_path)}, "position": {"line": line, "character": char}}; if extra_params: params.update(extra_params)` — explicitly do NOT use `{**extra_params, **base}` ordering that would let callers clobber `textDocument`/`position`.
- **Per-method coordinates (all 11):** `get_hover` `:511`, `get_completions` `:570` (note nested `result.get("items")` dispatch at `:586`), `get_references` `:617` (passes `extra_params={"context": {"includeDeclaration": include_declaration}}`), `get_definition` `:659`, `get_implementation` `:685`, plus `:715`, `:847`, `:915`, `get_declaration` `:1077` (KEEP the `is_unhandled_method_error` → `get_definition` fallback at `:1090-1091` in the caller, NOT in the helper), `get_type_definition` `:1105` (KEEP `is_unhandled_method_error` → `return []` branch at `:1118-1119`), `get_document_highlights` `:1133`, `prepare_rename` `:1163` (KEEP `is_unhandled_method_error` → `return None` at `:1176-1177` AND the post-result placeholder-from-file logic at `:1180+`).
- **Test target:** extend `tests/unit/test_pyright_lsp.py` (612 lines, flat async-def style with module-level `FakeLSPClient` fixture at `:25` and `PyrightClientHarness` at `:70`). Add a new parameterized `async def test_position_request_*` near the bottom — do NOT create a new test file, do NOT introduce a `class TestPositionRequest:` wrapper (the file convention is flat functions). Mirror `test_completion_mapping_returns_items` at `:288` and `test_declaration_and_type_definition_mapping` at `:466` for the `FakeLSPClient(...)` setup shape. Cover: (a) `textDocument/definition` (no extras), (b) `textDocument/hover` (no extras), (c) `textDocument/references` with `extra_params` carrying `context.includeDeclaration=True` — assert the recorded request payload contains exactly the expected `textDocument`, `position`, and `context` keys.
- **Edge cases the helper must NOT swallow:** (1) `is_unhandled_method_error` branches in `get_declaration`/`get_type_definition`/`prepare_rename` stay in the caller — the helper is unaware of LSP-method-unsupported semantics; (2) `prepare_rename`'s file-read placeholder fallback (`:1197+`) is post-dispatch and stays put; (3) `get_completions`' nested `result.get("items")` extraction stays in the caller; (4) `get_definition`/`get_declaration`/`get_type_definition`/`get_implementation` dual dict-or-list result handling stays in the caller. The helper returns the raw JSONDict response and nothing else.
- **Negative space:** do NOT touch the 9 file-only methods (`get_document_symbols` `:542`, `get_folding_ranges`, `get_semantic_tokens`, etc.) — they lack the `position` envelope and are out of scope. Do NOT modify `_request` `:184`, `ensure_file_open`, `path_to_uri`, `normalize_path`, or `PyrightError`. Do NOT change any public method signature, return type, or docstring. Do NOT add a backwards-compat shim or optional `position=None` overload — direct call-site rewrite only (private-repo policy).

</details>

### 4. cand-unused-symbol-sweep

| Field | Content |
|---|---|
| Status | merged (PR #57, 2026-05-29) |
| Backlog rows closed | cand-unused-symbol-sweep |
| Diagnosis | Both anchor files confirmed present and current. `src/python_refactor_mcp/tools/analysis/references.py` exports `find_references` (Pyright+Jedi merge, `_snap_to_symbol` fallback, `_add_context_lines` enrichment). `src/python_refactor_mcp/tools/search/dead_code.py` exports `dead_code_detection` — its `_iter_module_level_symbols` helper (line 49) deliberately skips decorated symbols (lines 62-65: `if node.decorator_list: continue`) and only checks module-level undecorated functions/classes/variables for zero-inbound-references. The proposed `unused_symbol_sweep` fills a distinct gap: it must cover the full export surface — `__all__`-listed names AND public (non-underscore) names — regardless of decoration, and flag any with zero cross-file inbound references. The distinction between the two tools is real and the backlog description is accurate. No existing implementation of `unused_symbol_sweep` found in `src/`. The registration table in `src/python_refactor_mcp/tool_registry.py` (`TOOL_RECORDS` tuple) has 83 `ToolRecord` entries; `server.py` has 8 explicit `@mcp.tool` wrappers; total registered = 91, matching `tests/unit/test_server.py:14` (`assert len(tools) == 91`). The new tool will bring the total to 92. |
| Approach | 1. Create `src/python_refactor_mcp/tools/search/unused_symbols.py` — implement `unused_symbol_sweep` as an `async def` mirroring the structure of `dead_code_detection`. Use `_iter_module_level_symbols` from `dead_code.py` as inspiration but remove the `if node.decorator_list: continue` guard and extend coverage to `__all__`-exported names. For each candidate symbol, call `pyright.get_references(str(path), line, char, include_declaration=False)` (same pattern as `_check_symbol` in `dead_code.py:161-199`) and filter to symbols with zero cross-file references. Return `PaginatedDeadCode` (reuses existing `DeadCodeItem` model). Parameters mirror `dead_code_detection`. 2. Re-export in `src/python_refactor_mcp/tools/search/__init__.py` — add the import and `"unused_symbol_sweep"` to `__all__`. 3. Register via the declarative table (post-#52 mechanism): add a pure-delegation `async def unused_symbol_sweep(ctx, ...)` to `src/python_refactor_mcp/tool_registry.py` (same five-line shape as the `dead_code_detection` delegate ~line 935: `app = _get_current_backends()` → `await search.unused_symbol_sweep(...)` → `await ctx.debug(...)` → `return result`), and append `ToolRecord(unused_symbol_sweep, _READONLY)` to `TOOL_RECORDS`. `PaginatedDeadCode` is already imported in `tool_registry.py`. Do NOT add a residual `@mcp.tool` wrapper in `server.py` — the body is pure delegation, so it belongs in the table. 4. Bump `tests/unit/test_server.py:14` from `== 91` to `== 92`. |
| Scope | Production files touched: 3 — `src/python_refactor_mcp/tools/search/unused_symbols.py` (new), `src/python_refactor_mcp/tools/search/__init__.py` (re-export), `src/python_refactor_mcp/tool_registry.py` (delegate + table entry). Test files modified: 1 — `tests/unit/test_server.py` (bump tool-count assertion 91 → 92) plus `test_unused_symbol_sweep_*` cases added to `tests/unit/test_search_tools.py` (same test-file budget). Rule 3: new structural unit (impl + re-export + registry entry) = 3 production files ≤ 4 cap. Rule 4 satisfied. NOTE: shares `tool_registry.py` + `tests/unit/test_server.py` with initiatives 5 and 6 — serialize (the second/third to land bump the count to 93/94). |
| Tool policy | edit-only |
| Estimated context cost | 50000 |
| Risks | (1) Decorated symbols: `dead_code_detection` intentionally skips `@mcp.tool`-decorated functions because decorators may register them externally. `unused_symbol_sweep` should skip `@mcp.tool`-decorated symbols in production source to avoid false positives — check for decorator names containing `mcp`/`tool`; document in the docstring. (2) `__all__` absent: treat all public (non-underscore) names as exported; handle both cases. (3) Concurrency: mirror `dead_code_detection`'s `asyncio.Semaphore(10)`. (4) Performance: a project-wide sweep calling `get_references` per exported symbol may be slow on large codebases — `limit` bounds response size but not computation; note "may be slow" in the docstring. (5) `test_doc_tool_count_drift.py` does NOT need updating — it polices doc files only and `reference.md` is in `ALLOWED_SOURCES`; still update `reference.md`'s "91 tools" as a doc-sync step (untested here). (6) Fanout probe skipped — additive new-file creation. |
| Validation | 1. `pytest tests/unit/test_server.py` — `test_server_registers_all_stage_one_tools` must pass with `len(tools) == 92`. 2. `pytest tests/unit/test_doc_tool_count_drift.py` — passes unchanged. 3. `pytest tests/` — full suite green. 4. `python -c "from python_refactor_mcp.tools.search import unused_symbol_sweep; print(unused_symbol_sweep)"` confirms the re-export resolves. 5. `grep -c 'ToolRecord(' src/python_refactor_mcp/tool_registry.py` returns 84 (was 83). 6. `python -m ruff check . && python -m mypy . && python -m pyright .` clean. |
| Performance review | N/A — new read-only tool, no hot-path changes to existing code. The tool itself may be slow on large codebases (noted in Risks) — inherent to per-symbol reference-counting, not a regression. |
| CHANGELOG category | Added |
| CHANGELOG entry (draft) | Added `unused_symbol_sweep` tool — project-wide audit of exported and private members with zero inbound cross-file references. Complements `dead_code_detection` (which scopes to undecorated module-level symbols only) by covering the full public API surface including `__all__`-exported and decorated members. Returns paginated `DeadCodeItem` results with confidence scores. |
| Backlog sync | Close rows: [cand-unused-symbol-sweep]. |

<details>
<summary>Sonnet handoff notes</summary>

**Sonnet handoff:**

- **Pattern coordinates (impl):** mirror `tools/search/dead_code.py:202-277` for `dead_code_detection`'s two-phase shape; reuse `_iter_module_level_symbols` `:49-112` (REMOVE the `if node.decorator_list: continue` guard at `:63-64`) and `_check_symbol` `:161-199` (per-symbol `pyright.get_references(str(path), line, char, False)` + cross-file filter). Keep `asyncio.Semaphore(10)` (`dead_code.py:220`). Import `name_position`/`python_files`/`PyrightSearchBackend` from `tools/search/_helpers.py`.
- **Registration coordinates (post-#52):** add the pure-delegation `async def unused_symbol_sweep(ctx, ...) -> PaginatedDeadCode` by mirroring the `dead_code_detection` delegate at `tool_registry.py:935-959`; append `ToolRecord(unused_symbol_sweep, _READONLY)` to the `TOOL_RECORDS` tuple before its close at `:1328` (mirror the `dead_code_detection` entry at `:1302`). `PaginatedDeadCode` already imported (`:62`) — no model-import edit. Do NOT add a `@mcp.tool` wrapper to `server.py`.
- **Re-export:** `tools/search/__init__.py` — add import (block `:3-7`) and `"unused_symbol_sweep"` to `__all__` (`:9-15`, keep alphabetical).
- **Test target:** bump `tests/unit/test_server.py:14` (`91`→`92`). Add `test_unused_symbol_sweep_*` to `tests/unit/test_search_tools.py` (do NOT create a new file); mirror `test_dead_code_detection_marks_unreferenced_symbols` at `:73-93` (`AsyncMock` pyright + `_config(tmp_path)` from `tests.helpers`).
- **Edge cases to cover:** (1) skip `mcp`/`tool`-decorated symbols (the only intentional behavioral divergence from `dead_code`'s blanket decorator-skip — this tool covers decoration EXCEPT external registrars); (2) `__all__` present → use it; `__all__` absent → all non-underscore names; (3) zero-reference detection must filter cross-file only (same-file refs are not "unused export").
- **Negative space:** do NOT modify `dead_code.py` or its `dead_code_detection` delegate/`ToolRecord` — this is an additive sibling, not an edit of the existing tool. Do NOT touch `test_doc_tool_count_drift.py` (doc-file-only).
- **Follow-up (post-ship, do NOT expand this initiative's budget):** copying `_iter_module_level_symbols` minus the decorator guard duplicates a ~60-line walker; after this tool exists, extract a shared `_iter_module_level_symbols(path, *, skip_decorated)` into `_helpers.py` (candidate row `search-symbol-iter-dedup`).

</details>

### 5. cand-extract-superclass

| Field | Content |
|---|---|
| Status | merged (PR #58, 2026-05-29) |
| Backlog rows closed | cand-extract-superclass |
| Diagnosis | No `extract_superclass` symbol exists anywhere under `src/` (confirmed via grep — zero hits). Rope 1.14 ships no `ExtractSuperclass` refactoring (backlog assertion holds). The CST apply foundation is live at `src/python_refactor_mcp/util/cst_apply.py` — `apply_cst_transformer` at line 58 and `apply_cst_transformer_batch` at line 95. The foundation contract: read file → build transformer → `module.visit(transformer)` → emit a whole-file replace `TextEdit` if source changed; `write_atomic` on `apply=True`. There are currently zero consumers of `apply_cst_transformer` in `src/` — `superclass.py` will be the first. The `TOOL_RECORDS` table in `src/python_refactor_mcp/tool_registry.py` currently holds 83 entries; eight residual `@mcp.tool` wrappers live in `server.py` after `register_tools(mcp)` (line 294). The new tool's body is pure delegation (no conditional branching), so it belongs in the `TOOL_RECORDS` table, not as a residual wrapper. `tools/refactoring/__init__.py` exports 33 names; `extract_superclass` must be added to the import and `__all__`. The tool-count gate in `tests/unit/test_server.py:14` hard-codes `len(tools) == 91`; adding one tool requires bumping to 92. CST member-hoist logic has multiple plausible design branches (decorators, `@property`, `@classmethod`/`@staticmethod`, `__slots__`, `__init__` instance attributes). |
| Approach | 1. Create `src/python_refactor_mcp/tools/refactoring/superclass.py`. Implement `class ExtractSuperclassTransformer(cst.CSTTransformer)` accepting `source_class`, `base_class_name`, `members`. It visits `cst.ClassDef`: when the name matches, collect the listed `FunctionDef`/class-level-assignment members, append `base_class_name` to `bases`, remove the members from the original body, and raise `BackendError` if a requested member is missing, decorated with `@classmethod`/`@staticmethod`/`@property`, or is an `__init__` instance attribute. Insert a new `ClassDef` for the superclass (hoisted members) immediately before the original class. Public `async def extract_superclass(pyright, file_path, class_name, base_class_name, members, apply=False) -> RefactorResult`: call `apply_cst_transformer(...)`, wrap via `result_from_text_edits` (`tools/refactoring/helpers.py`), then `await post_apply_diagnostics(pyright, result)` only when `apply=True` (mirror `type_annotations.py:100-103` and the foundation contract at `util/cst_apply.py:58-92`). 2. Add `from .superclass import extract_superclass` to `tools/refactoring/__init__.py` and append to `__all__`. 3. Register via the declarative table (post-#52 mechanism): add a pure-delegation `async def extract_superclass(ctx: MCPContext, file_path, class_name, base_class_name, members, apply=False) -> RefactorResult` to `src/python_refactor_mcp/tool_registry.py` alongside the other refactoring delegates (body: `app = _get_current_backends()` → `await refactoring.extract_superclass(app.pyright, ...)` → `await ctx.debug(...)` → `return result`), and append `ToolRecord(extract_superclass, _DESTRUCTIVE)` to `TOOL_RECORDS`. `RefactorResult` is already imported in `tool_registry.py`. Do NOT add a residual `@mcp.tool` wrapper in `server.py` — pure delegation belongs in the table. 4. Bump `tests/unit/test_server.py:14` from `== 91` to `== 92`. 5. Extend `tests/unit/test_refactoring_tools.py` with `test_extract_superclass_preview_*` / `_apply_*` cases (`tmp_path` + `AsyncMock` pyright, mirror `test_apply_type_annotations_preview_does_not_write`/`_apply_writes_and_refreshes` ~lines 742/766) plus at least one rejection-path test (member not found → `BackendError`). |
| Scope | Production files touched: 3 — `src/python_refactor_mcp/tools/refactoring/superclass.py` (new), `src/python_refactor_mcp/tools/refactoring/__init__.py` (add export), `src/python_refactor_mcp/tool_registry.py` (delegate + `ToolRecord`). Test files: `tests/unit/test_refactoring_tools.py` (extend) + `tests/unit/test_server.py` (count bump; same test-file budget). Rule 3: 3 production files ≤ 4 cap; no exemption needed. `server.py` NOT touched (post-#52 the tool goes in the table). NOTE: shares `tool_registry.py` + `tests/unit/test_server.py` with initiatives 4 and 6 — serialize; the second/third to land bumps the count to 93/94. |
| Tool policy | edit-only |
| Estimated context cost | 50000 |
| Risks | (1) CST member-hoist has multiple plausible design branches — `@classmethod`/`@staticmethod`/`@property` decorators, class-level assignments vs `__init__` instance attributes, `__slots__` references, nested class bodies. The transformer must explicitly reject (raise `BackendError`) any member in these categories rather than silently mishandle. (2) Insertion position: the new superclass `ClassDef` must precede the original class in the module body — `leave_Module` is the correct hook; if using `leave_ClassDef` + `FlattenSentinel`, avoid double-emit. (3) `tool_registry.py` has an `eval_str=True` annotation-import-scope contract — any new return types not already imported must be added; `RefactorResult` is already imported. (4) `test_server.py` count bump to 92 is mandatory; CI fails without it. (5) Fanout probe skipped — new-file addition, delegate called from zero existing sites. |
| Validation | (a) `pytest tests/unit/test_refactoring_tools.py -k extract_superclass -v` — preview: `RefactorResult` with correct `edits`, `notify_file_changed` NOT awaited; apply: awaited + disk updated; rejection: `BackendError` when member absent. (b) `pytest tests/unit/test_server.py -v` — passes with count == 92. (c) `pytest tests/unit/test_cst_apply.py -v` — foundation unchanged. (d) `pytest tests/ -x` — full suite green; no import errors. (e) `python -m ruff check . && python -m mypy . && python -m pyright .` clean. (f) Manual smoke: `class Foo:` with two methods, `extract_superclass(members=["bar"])` preview → new base class created, `Foo` retains `baz` + gains the base in `bases`. |
| Performance review | N/A — correctness fix, no hot-path changes. CST parse + transform + emit on a single file; no startup/background impact. |
| CHANGELOG category | Added |
| CHANGELOG entry (draft) | Added `extract_superclass` tool — pull a named subset of methods and class-level fields from an existing class up into a new base class using the LibCST foundation. Defaults to preview mode (`apply=False`); set `apply=True` to write. Raises an error for unsupported member kinds (`@classmethod`/`@staticmethod`/`@property` or `__init__` instance attributes). Related: `extract_method`, `move_symbol`. |
| Backlog sync | Close rows: [cand-extract-superclass]. |

<details>
<summary>Sonnet handoff notes</summary>

**Sonnet handoff:**

- **Foundation contract (NOT a sibling-mirror — there are ZERO existing `apply_cst_transformer` consumers):** build the transformer against `util/cst_apply.py:58-92` directly. Contract: `apply_cst_transformer(file_path, transformer, apply=...)` reads the file, runs `module.visit(transformer)`, returns `([], [])` on no-op, else one whole-file-replace `TextEdit` + writes atomically when `apply=True`. `parse_module` (`:36`) already wraps syntax errors as `BackendError`. The `cst_apply.py` module docstring (lines 4-6) names `extract_class`/`convert_to_dataclass` as consumers — they do NOT exist in the tree; ignore them, do not search for them as templates.
- **Result-wrapper coordinates (the ONLY part `type_annotations.py` is a valid template for):** mirror `type_annotations.py:99-103` — `result_from_text_edits(edits, description, apply)` then `if apply: return await post_apply_diagnostics(pyright, result)`. Both helpers live at `tools/refactoring/helpers.py:115` and `:131`. Note `type_annotations.py:9-14` states it deliberately does NOT use the CST foundation — copy only its wrapper tail, not its hint-walking body.
- **Registry delegate (post-#52 — table, NOT a `server.py` wrapper):** add a pure-delegation `async def extract_superclass(ctx, file_path, class_name, base_class_name, members, apply=False) -> RefactorResult` to `tool_registry.py` next to the refactoring delegates (mirror `move_symbol` at `:524-535` for shape: `app = _get_current_backends()` → `await refactoring.extract_superclass(app.pyright, ...)` → `await ctx.debug(...)` → `return result`), then append `ToolRecord(extract_superclass, _DESTRUCTIVE)` to `TOOL_RECORDS` (table ends `:1327`, before the closing paren `:1328`). `RefactorResult` is already imported (`:68`); `_DESTRUCTIVE` at `:103`. `eval_str=True` (docstring `:27-30`) means any NEW return type must be imported — `RefactorResult` already is, so no import edit needed.
- **Export:** add `from .superclass import extract_superclass` to `tools/refactoring/__init__.py` after the `type_annotations` import (`:29`); insert `"extract_superclass"` into `__all__` between `"expand_star_imports"` and `"extract_method"` (`:38`). The list currently holds 32 names, not 33 as the stanza states — do not hunt for a phantom 33rd.
- **Tool-count gate:** bump `tests/unit/test_server.py:14` from `assert len(tools) == 91` to `== 92`. If initiatives 4/6 land first, the count is already 92/93 — read the current value, do not blindly write 92.
- **Test target:** extend `tests/unit/test_refactoring_tools.py` (do NOT create a new file). Mirror `test_apply_type_annotations_preview_does_not_write` at `:742` and `_apply_writes_and_refreshes` at `:766` — same `tmp_path` + `AsyncMock()` pyright shape. Preview asserts `result.applied is False`, disk unchanged, `pyright.notify_file_changed.assert_not_awaited()`; apply asserts disk rewritten + `pyright.notify_file_changed.assert_awaited()` (set `pyright.get_diagnostics.return_value = []`). Add at least one rejection test asserting `pytest.raises(BackendError)`.
- **Edge cases the transformer MUST reject with `BackendError` (not silently mishandle):** (1) member not found in the class body; (2) member decorated `@classmethod`/`@staticmethod`/`@property` — inspect `FunctionDef.decorators`; (3) `__init__`-assigned instance attributes (`self.x = ...`) requested as members; (4) `__slots__` entries. Only plain `FunctionDef` and class-level assignments hoist. Insertion: emit the new base `ClassDef` immediately BEFORE the original class — use `leave_Module` to splice the body; if you instead use `leave_ClassDef` + `FlattenSentinel`, guard against double-emit.
- **Negative space:** do NOT modify `util/cst_apply.py` (foundation is frozen — validation runs `test_cst_apply.py` unchanged). Do NOT add a residual `@mcp.tool` wrapper in `server.py` — pure delegation belongs in the table only. Do NOT add a backwards-compat shim or optional dual code path (private-repo correctness-over-cheapness policy).

</details>

### 6. cand-test-impact-selector

| Field | Content |
|---|---|
| Status | in-progress (branch: remediation/cand-test-impact-selector, worktree: .worktrees/cand-test-impact-selector) |
| Backlog rows closed | cand-test-impact-selector |
| Diagnosis | The backlog row cites `src/python_refactor_mcp/tools/navigation/call_hierarchy.py` — that file does not exist. The correct path is `src/python_refactor_mcp/tools/navigation/hierarchy.py`, which defines `call_hierarchy()` (line 107) and `_traverse_hierarchy()` (line 36). `call_hierarchy()` returns a `CallHierarchyResult` with `.callers`/`.callees` lists of `CallHierarchyItem` (each carrying `file_path`, `name`, `kind`, `range`). A complementary test-path heuristic exists in `src/python_refactor_mcp/tools/metrics/test_map.py` (lines 60-63): a reference is test-touching when `"test"` appears in the filename. `test_impact_select` must (a) accept a list of `(file_path, line, character)` anchors, (b) per anchor call `call_hierarchy` with `direction="callers"`, (c) filter callers to test files, (d) emit pytest node IDs. No existing model covers this output shape — a new `TestImpactResult` (+`TestImpactEntry`) Pydantic model is needed in `models.py`. Tool registration must target the `TOOL_RECORDS` table in `src/python_refactor_mcp/tool_registry.py` (the declarative path since PR #52; pure-delegation shape). The tool-count gate in `tests/unit/test_server.py:14` asserts `len(tools) == 91` and must be bumped to 92. |
| Approach | 1. **`src/python_refactor_mcp/models.py`** — append `TestImpactEntry` (`symbol: str`, `affected_test_files: list[str]`, `pytest_node_ids: list[str]`, `truncated: bool`) and `TestImpactResult` (`entries: list[TestImpactEntry]`, `total_affected_tests: int`, `truncated: bool`). Pytest node-ID derivation: `<file_path>::<class>::<func>` for class methods, `<file_path>::<func>` for module-level — parse `CallHierarchyItem.name`/`detail`; parametrized/nested tests are heuristic (document in the model docstring). 2. **`src/python_refactor_mcp/tools/analysis/test_impact.py`** (NEW) — `async def test_impact_select(pyright, symbols: list[dict], depth: int = 2, max_items: int = 200) -> TestImpactResult`. Per anchor call `navigation.call_hierarchy(pyright, file_path, line, character, direction="callers", depth=depth, max_items=max_items)`; filter `result.callers` via the `test_map.py:60-63` predicate (reuse verbatim); build node IDs; dedupe per entry with `sorted(set(...))`. 3. **`src/python_refactor_mcp/tools/analysis/__init__.py`** — add the import + append `"test_impact_select"` to `__all__`. 4. Register via the declarative table (post-#52 mechanism): add `TestImpactResult` to the `tool_registry.py` model-import block, add a pure-delegation `async def test_impact_select(ctx, ...) -> TestImpactResult` (body: `app = _get_current_backends()` → `await analysis.test_impact_select(app.pyright, ...)` → `await ctx.debug(...)` → `return result`), and append `ToolRecord(test_impact_select, _READONLY)` to `TOOL_RECORDS`. Do NOT add a residual `@mcp.tool` wrapper in `server.py`. 5. Bump `tests/unit/test_server.py:14` from `== 91` to `== 92`. |
| Scope | Production files (4): `src/python_refactor_mcp/models.py`, `src/python_refactor_mcp/tools/analysis/test_impact.py` (new), `src/python_refactor_mcp/tools/analysis/__init__.py`, `src/python_refactor_mcp/tool_registry.py` (delegate + `ToolRecord`). Test files (2): `tests/unit/test_server.py` (count bump), `tests/unit/test_test_impact.py` (new). Rule 3: 4 production files — at the hard cap; new structural unit (model + impl + analysis-package export + registration table entry). Rule 4: 2 test files ≤ 3. `server.py` NOT touched (post-#52). NOTE: shares `tool_registry.py` + `tests/unit/test_server.py` with initiatives 4 and 5 — serialize; the second/third to land bumps the count to 93/94. |
| Tool policy | edit-only |
| Estimated context cost | 55000 |
| Risks | (1) Pytest node-ID derivation from `CallHierarchyItem.name`+`detail` is heuristic: parametrized (`test_foo[param]`) and nested-class tests won't match pytest's collected format without AST parsing — document as best-effort v1, return raw node IDs (mirror the simple `test_map.py` pattern, do not over-engineer). (2) `call_hierarchy` with `direction="callers"` may return 0 results for symbols Pyright can't resolve without a warmed workspace — handle empty `.callers` gracefully and still return a valid `TestImpactResult`. (3) `tool_registry.py` is ~1363 lines; the delegate + table entry are additive but verify formatting with `ruff check` after edit. (4) Anchor was stale (`call_hierarchy.py` → `hierarchy.py`); the path above is verified. (5) `test_doc_tool_count_drift.py` does NOT need updating — plan.md lives under `ai_docs/plans/` in its `EXCLUDED_SUBTREES`; still update `reference.md`'s tool count as a doc-sync step. |
| Validation | (1) `pytest tests/unit/test_server.py` — passes with `len(tools) == 92`. (2) `pytest tests/unit/test_test_impact.py` — new tests: (a) mock pyright callers incl. a synthetic test-file path → result includes the expected node ID; (b) non-test callers excluded; (c) empty-callers → `TestImpactResult(entries=[], total_affected_tests=0, truncated=False)`. (3) `python -m ruff check . && python -m mypy . && python -m pyright .` clean. (4) `pytest tests/ -x` full suite green. (5) Manual smoke: call `test_impact_select` with a known anchor (e.g. `call_hierarchy` in `hierarchy.py`) and verify a non-empty `entries` list. |
| Performance review | N/A — correctness fix, no hot-path changes. The tool calls `call_hierarchy` once per anchor; callers are I/O-bound on the Pyright LSP. No hot-path concerns at v1. |
| CHANGELOG category | Added |
| CHANGELOG entry (draft) | Added `test_impact_select` tool — given a list of changed symbol anchors (file, line, character), traverses the call-hierarchy callers graph and returns the pytest node IDs of test files that transitively exercise those symbols. |
| Backlog sync | Close rows: [cand-test-impact-selector]. |

<details>
<summary>Sonnet handoff notes</summary>

**Sonnet handoff:**

- **Registry mechanism (read first):** `tool_registry.py` delegates are real `async def` functions, NOT `(name, callable, fmt)` data tuples (see module docstring `:10-21`). Add `async def test_impact_select(ctx, ...)` mirroring `dead_code_detection` at `:935-959` (pure-delegation shape), then append `ToolRecord(test_impact_select, _READONLY)` to the `TOOL_RECORDS` tuple before its close at `:1328`. `analysis` is already imported (`:86`); add `TestImpactResult` to the model-import block `:42-85`. Do NOT add a `server.py` wrapper.
- **call_hierarchy call signature:** `navigation.call_hierarchy(pyright, file_path, line, character, direction, depth, max_items)` — positional, `direction="callers"`; returns `CallHierarchyResult` with `.callers` list. Mirror the delegate's own call at `tool_registry.py:344`.
- **Test-file predicate (reuse verbatim):** `test_map.py:60-63` — `"test" in Path(ref.file_path).name.lower() or "tests" in str(ref.file_path).lower()`. Apply to each caller's `.file_path`.
- **Model append point:** add `TestImpactEntry` + `TestImpactResult` after `SecurityScanResult` at `models.py:589` (end of file).
- **Node-ID edge case (critical):** `CallHierarchyItem` (`models.py:185-193`) has ONLY `name`, `kind`, `file_path`, `range`, `detail` — there is NO separate class/function field. You cannot reliably build `file::Class::method` from this; `name` is bare (e.g. `"caller"`) and `detail` is optional/often None. Per Risk (1): emit `<file_path>::<name>` best-effort, document parametrized/nested as heuristic, do NOT AST-parse. Handle `detail is None`.
- **Empty-callers path:** `call_hierarchy` returns `callers=[]` for unresolvable symbols (placeholder branch, `hierarchy.py:186-198`) — return a valid `TestImpactResult(entries=[], total_affected_tests=0, truncated=False)`, never raise.
- **Test target:** create `tests/unit/test_test_impact.py` (no `test_hierarchy*` file exists). Mirror the `_item()` helper at `tests/unit/test_navigation_tools.py:24-33` and `test_call_hierarchy_collects_depth_for_both_directions` at `:37-66` for the `AsyncMock()` pyright setup (`prepare_call_hierarchy.return_value`, `get_incoming_calls.side_effect`). Synthesize a caller with a `/tests/` path for the positive case.
- **Count gate:** bump `tests/unit/test_server.py:14` `== 91` → `== 92`. NOTE: shared with initiatives 4 and 5 — if either landed first, the current value is 92/93; read the line, do not assume 91.
- **Negative space:** do NOT modify `call_hierarchy`/`_traverse_hierarchy` in `hierarchy.py` (`:36`, `:107`) — consume them only. Do NOT touch `test_map.py` (`get_test_coverage_map`) — copy its predicate, don't import its function. Do NOT add a `server.py` `@mcp.tool` wrapper. (Predicate-duplication note: this is the 2nd copy of the `test_map.py` heuristic; if a 3rd appears, extract a shared `_is_test_path` helper — out of scope here.)

</details>
