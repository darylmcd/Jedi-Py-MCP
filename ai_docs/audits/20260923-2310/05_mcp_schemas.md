# 05 — C4 Schema audit

Evidence: `raw/schema_audit.json` (generated by `harness/schema_audit.py.txt` over the live `tools/list` payloads).

| Check | Result |
|---|---|
| Input schemas valid JSON Schema (Draft 2020-12 `check_schema`) | **108/108 valid** |
| Output schemas present + valid | **108/108** present and valid; 0 runtime violations across 383 ok calls (`02_mcp_tools.md`) |
| Required fields enforced | **Yes** — every `missing_all_required` / `missing:<field>` / `null:<field>` call rejected with a pydantic validation error naming the field (430 such rejections, 0 accepted) |
| Wrong types rejected | **Yes** for int/str/list/bool/object (lax coercion only for numeric strings, as expected from pydantic) |
| Descriptions non-empty | 108/108 tool descriptions; **0/101** tools with parameters have any per-parameter `description` |
| Enum values documented in schema | **No** — 8 closed-set string params typed as bare `string` |
| Naming convention | 108/108 `snake_case`, verb-first; consistent |
| Annotations present | 108/108 carry `readOnlyHint`/`destructiveHint`/`idempotentHint`/`openWorldHint` |

## Findings

| ID | Sev | Finding | Evidence | Anchor |
|---|---|---|---|---|
| mcp-surface-param-schema-docs | low | No parameter carries a schema `description`; semantics (0-based positions, `layers` ordering, `target_modules` path→symbols map, `SignatureOperation` field meaning per `op`) live only in the tool prose. Closed sets are bare `string` with no `enum`: `call_hierarchy.direction`, `type_hierarchy.direction`, `docstring_sync.style`, `generate_code.kind`, `get_diagnostics.severity_filter`, `get_documentation.source`, `structural_search.language`, `SignatureOperation.op`. `refactor_transaction.steps` is `list[object]` with `additionalProperties:true` although the step shape `{tool: enum, args: object}` is fixed. Top-level `line`/`character` lack `minimum: 0` (only `$defs.Position` has it), so negatives pass schema and fail later as backend errors. | `raw/schema_audit.json` → `no_param_descriptions` (101 entries), `enumlike_str` (8), `untyped_object` | `tool_registry.py:354` (call_hierarchy), `:397`, `:783`, `:1143`, `:224`, `:181`, `:1198`, `:1442`; `models.py:323` (`SignatureOperation`) |
| mcp-surface-annotation-hints | low | `destructiveHint:false` ("additive") on tools whose `apply=True` path rewrites existing content with whole-file replace edits: `format_code`, `apply_lint_fixes` (can delete code), `apply_type_annotations`, `apply_code_action`, `organize_imports`, `expand_star_imports`, `relatives_to_absolutes`, `froms_to_imports`, `handle_long_imports`. MCP defines additive as "only adds"; hosts use the hint for confirmation policy. | live `annotations` in `raw/inventory_refactoring.json` | `tool_registry.py:1676-1684` (`ADDITIVE_ANNOTATIONS` records), `:108` |
| mcp-surface-unknown-args-ignored | low | Schemas omit `additionalProperties:false`; unknown keys accepted by 98/101 tools (see `02_mcp_tools.md`). | `02_mcp_tools.md` | SDK-generated arg models |
