# bl-0014 — Nonexistent scope reads as clean

**row:** `bl-0014` · **pri:** `Medium` · **size:** `M`

## Anchors

- `src/python_refactor_mcp/tools/search/_helpers.py:122-143`
- `src/python_refactor_mcp/tools/navigation/outline.py:121`
- `src/python_refactor_mcp/tools/metrics/coverage.py`
- `tests/unit/test_search_tools.py`

## Acceptance

- [ ] `dead_code_detection{root_path:<missing>}` returns a `scan_failures` entry
- [ ] `get_symbol_outline{file_path:<missing>}` errors or reports the failure
- [ ] `get_type_coverage` on a missing file does not headline `100.0`

## Evidence

- `dead_code_detection` with a nonexistent root → `{"items":[],"scan_failures":[]}`; peers (`code_metrics`, `security_scan`) already report `FileNotFoundError` scan failures. — see `audit-reports/20260923-2310_jedi-py-mcp_mcp-surface-audit.md` and `ai_docs/audits/20260923-2310/`

## Context

- Align with the existing `scan_failures` convention used by the metrics tools.
