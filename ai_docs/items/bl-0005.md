# bl-0005 — selection_range / inlay hints / semantic tokens silently empty

**row:** `bl-0005` · **pri:** `High` · **size:** `M`

## Anchors

- `src/python_refactor_mcp/backends/pyright_lsp.py:321-350`
- `src/python_refactor_mcp/tools/refactoring/type_annotations.py`
- `src/python_refactor_mcp/server.py:112-140`
- `tests/integration/test_end_to_end.py:408-515`
- `tests/unit/test_pyright_lsp.py`

## Acceptance

- [ ] against the pinned Pyright, `selection_range`, `get_inlay_hints`, `get_semantic_tokens` return either real data (fallback) or an explicit unsupported error — never a silent `[]`
- [ ] `apply_type_annotations` annotates an unannotated function in the fixture, or states why it cannot
- [ ] integration tests assert non-empty payloads or the explicit error, not merely `isinstance(list)`

## Evidence

- Pyright 1.1.411 answers `-32601 Unhandled method` for `textDocument/selectionRange`, `/inlayHint`, `/semanticTokens/full`; the backend maps that to `[]` (pyright_lsp.py unhandled-method branches at 1023/1245/1304). — see `audit-reports/20260923-2310_jedi-py-mcp_mcp-surface-audit.md` and `ai_docs/audits/20260923-2310/`

## Context

- 13 call sites in `pyright_lsp.py` convert "Unhandled method" into an empty result; the client never stores the server's advertised capabilities. Hiding unsupported tools from `tools/list` is an acceptable alternative to the explicit error.
