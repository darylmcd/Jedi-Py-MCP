# bl-0009 — Dead-code sweeps exceed 30 s on a 137-file repo

**row:** `bl-0009` · **pri:** `Medium` · **size:** `M` · **deps:** `bl-0001`

## Anchors

- `src/python_refactor_mcp/tools/search/dead_code.py`
- `src/python_refactor_mcp/tools/search/unused_symbols.py`
- `src/python_refactor_mcp/tools/search/_helpers.py`
- `tests/unit/test_search_tools.py`

## Acceptance

- [ ] both tools finish < 10 s on a copy of this repo (audit baseline: 30.4 s / 39.7 s)
- [ ] result sets are unchanged versus the serial implementation

## Evidence

- Repo-scale timing pass: `dead_code_detection` 30.39 s, `unused_symbol_sweep` 39.72 s. — see `audit-reports/20260923-2310_jedi-py-mcp_mcp-surface-audit.md` and `ai_docs/audits/20260923-2310/`

## Context

- Depends on bl-0001 (same file, correctness fix first).
2026-09-24 (plan 20260924T183010Z, PR #146 closed): profiled on a 142-file copy — dead_code 786 / unused_symbol_sweep 432 references requests; a name-occurrence pre-filter removed only 5 / 26 of them, so request count is not the lever. Cold 10-48 s vs warm 2-18 s: cost is Pyright first-request program analysis plus the up-to-2 s per-file publishDiagnostics wait in dead_code Phase 1 (pyright_lsp.py get_diagnostics ~:501-505). Next slice: remove/bound the Phase-1 diagnostics wait and measure; then consider a session prewarm. Result-key diffs only differed on references timeouts under load (cold-run nondeterminism).
2026-09-25 (plan 20260925T141731Z, held at measure-first ABORT, no PR): at HEAD 4cd6350 dead_code_detection had 0 Phase-1 diagnostics-wait timeouts over 5 cold+warm runs. 97-file corpus: cold median 5.71 s (P1 3.50 / P2 1.93), warm 2.34 s; 143-file probe cold 7.75-8.15 s (P1 ~3.6 / P2 ~3.9). P1 is real Pyright first-check time, not the 2 s wait, so batching/overlap was dropped. dead_code_detection already meets < 10 s cold; the open work is unused_symbol_sweep (39.7 s baseline, cold references cost) via a session prewarm.
