# bl-0046 — Backend value normalization unreachable after bl-0021 Literal typing

**row:** `bl-0046` · **pri:** `Low` · **size:** `M`

## Anchors

- `src/python_refactor_mcp/tools/navigation/hierarchy.py:27`
- `src/python_refactor_mcp/tools/navigation/hierarchy.py:138`
- `src/python_refactor_mcp/tools/navigation/hierarchy.py:250-251`
- `src/python_refactor_mcp/tools/analysis/diagnostics.py:54`
- `src/python_refactor_mcp/tools/refactoring/docstrings.py:155`
- `tests/unit/test_navigation_tools.py`

## Acceptance

- [ ] MCP validation and the backend accept the same value set: remove the `callers`/`callees` alias map and `strip().lower()` normalization, or retype the backend parameters to the shared `tool_params` Literal aliases
- [ ] A unit test pins the backend behaviour for internal callers (e.g. `tools/analysis/test_impact.py`)

## Evidence

- `hierarchy.py:27` at HEAD `4c36c97`: `_TYPE_DIRECTION_ALIASES: dict[str, str] = {"callers": "supertypes", "callees": "subtypes"}`; `:250-251` `_TYPE_DIRECTION_ALIASES.get(direction.strip().lower(), direction.strip().lower())`. `tool_params.py:135` `TypeDirection = Annotated[Literal["supertypes", "subtypes", "both"], ...]` — the aliases and case folding can no longer be reached through MCP.

## Context

- Spin-off from /backlog-remediate plan `20260925T141731Z_backlog-remediate` (bl-0021 deepener + cold review sketch).
