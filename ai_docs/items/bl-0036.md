# bl-0036 — Rope caller-argument validation raises RopeError

**row:** `bl-0036` · **pri:** `Medium` · **size:** `S` · **deps:** `bl-0035`

## Anchors

- `src/python_refactor_mcp/backends/rope_backend.py:110-170`
- `src/python_refactor_mcp/backends/rope_backend.py:285-330`
- `src/python_refactor_mcp/backends/rope_backend.py:505-580`
- `tests/unit/test_rope_backend.py`

## Acceptance

- [ ] negative/out-of-range line, character and offset, path outside the workspace, non-Python source, missing `change_signature` operation arguments, and invalid `split_module` targets each return `[INVALID_INPUT]` naming the parameter
- [ ] `rope backend is not initialized`, parse and snapshot failures stay `RopeError`

## Evidence

- `rope_backend.py:117-169` (change_signature op args), `:291-325` (path/position/offset), `:509-527` (source file role), `:553-579` (split_module targets) all `raise RopeError` for caller-supplied input, redacted to "Refactoring backend failed".

## Context

- Split from `bl-0003` (plan 20260924T183010Z_backlog-remediate). Same file as `bl-0035`; land after it.
