# bl-0025 — Five more LSP requests silently return [] when Pyright lacks the method

**row:** `bl-0025` · **pri:** `Medium` · **size:** `S`

## Anchors

- `src/python_refactor_mcp/backends/pyright_lsp.py`
- `tests/unit/test_pyright_lsp.py`

## Acceptance

- [ ] Each remaining branch raises `[LSP_UNSUPPORTED]` (or is proven supported by the pinned Pyright and documented)
- [ ] Unit test per branch

## Evidence

- `pyright_lsp.py:970`, `:986`, `:1002`, `:1147`, `:1413` each `if is_unhandled_method_error(response): return []` at HEAD `20b565c`. bl-0005 fixed selectionRange/inlayHint/semanticTokens only.

## Context

- Spin-off from /backlog-remediate plan `20260924T131340Z_backlog-remediate` (closeout, evidence re-verified at HEAD `20b565c`).
