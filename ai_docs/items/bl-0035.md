# bl-0035 — Rope change-stack/history/transaction misuse reported as backend outage

**row:** `bl-0035` · **pri:** `High` · **size:** `M`

## Anchors

- `src/python_refactor_mcp/backends/rope_backend.py:1291-1320`
- `src/python_refactor_mcp/backends/rope_backend.py:1345-1470`
- `src/python_refactor_mcp/tools/composite.py:116-145`
- `tests/unit/test_refactor_transaction.py`
- `tests/unit/test_rope_backend.py`

## Acceptance

- [ ] `refactor_transaction{steps:[]}`, a malformed step, and an unsupported tool return the specific pre-flight reason as `[INVALID_INPUT]`
- [ ] `undo_refactoring` / `redo_refactoring` on empty history, `commit_change_stack` / `rollback_change_stack` without begin, and `begin_change_stack` twice each return a state-specific `[INVALID_INPUT]` message
- [ ] genuine rope failures keep the redacted `[ROPE_BACKEND] … Failure ID` shape

## Evidence

- `rope_backend.py:1353`, `:1362`, `:1405` (stack state) and `:1443-1467` (transaction pre-flight) raise `RopeError`; `undo`/`redo` (`:1291-1320`) let rope's empty-history error surface as a backend failure. — see `audit-reports/20260923-2310_jedi-py-mcp_mcp-surface-audit.md`

## Context

- Split from `bl-0003` (plan 20260924T183010Z_backlog-remediate). `composite.py` docstring states pre-flight errors raise `RopeError` — update it with the change.
