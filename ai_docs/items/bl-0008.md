# bl-0008 — Failure IDs are untraceable

**row:** `bl-0008` · **pri:** `Medium` · **size:** `S`

## Anchors

- `src/python_refactor_mcp/__main__.py`
- `tests/unit/test_main.py`

## Acceptance

- [ ] a forced backend failure's stderr line carries `failure_id`, `exception_types`, and `traceback_locations`
- [ ] output stays free of request paths/payloads (redaction contract of `_safe_failure_diagnostics` unchanged)

## Evidence

- 729 `Backend failure id=… tool=… code=…` stderr lines in the audit run, none with exception detail; the `generate_code` root cause could only be found by calling the backend directly. — see `audit-reports/20260923-2310_jedi-py-mcp_mcp-surface-audit.md` and `ai_docs/audits/20260923-2310/`

## Context

- `tool_runtime.py:220-233` already builds the structured `extra`; nothing under `src/` configures a formatter to emit it.
