# bl-0003 — Caller-input errors reported as backend outages

**row:** `bl-0003` · **pri:** `High` · **size:** `M` · **deps:** `bl-0002`

## Anchors

- `src/python_refactor_mcp/errors.py:14-52`
- `src/python_refactor_mcp/backends/pyright_lsp.py:527-545`
- `src/python_refactor_mcp/backends/rope_backend.py`
- `src/python_refactor_mcp/tools/composite.py:116-160`
- `tests/unit/test_pyright_lsp.py`
- `tests/unit/test_refactor_transaction.py`

## Acceptance

- [ ] `get_type_info{line:-1}` and a nonexistent `file_path` return an input error naming the parameter, with no "check server_status and retry" advice
- [ ] `refactor_transaction{steps:[]}` / malformed step / unsupported tool return the specific pre-flight reason
- [ ] `undo_refactoring` / `redo_refactoring` on empty history, `commit_change_stack` / `rollback_change_stack` without begin, and `begin_change_stack` twice each return a state-specific message
- [ ] genuine backend failures keep the redacted `[CODE] … Failure ID` shape

## Evidence

- 397 calls answered caller mistakes with `[PYRIGHT_BACKEND]`/`[ROPE_BACKEND]` "backend failed … retry"; the 2026-06-19 audit observed `File not found: …` for the same probe (regression via e93207e #93). — see `audit-reports/20260923-2310_jedi-py-mcp_mcp-surface-audit.md` and `ai_docs/audits/20260923-2310/`

## Context

- `_position_request` already raises `PyrightError` for out-of-range coordinates on purpose (docstring: "so a bad coordinate is distinguishable") — the blanket `caller_summary` redaction erased that distinction.
2026-09-24 (plan 20260924T131340Z): bl-0002 shipped `ToolInputError` (errors.py) and bl-0012 made `run_in_thread` re-raise it unwrapped. Also convert the remaining first-party caller-input `ValueError` sites: `tools/search/structural.py`, `tools/navigation/hierarchy.py`, `tools/refactoring/rename.py`, `tools/navigation/outline.py`, `tools/analysis/diagnostics.py`, `tools/analysis/type_users.py`, `tools/search/_helpers.py`, `server.py` — these may push this row to a split.
