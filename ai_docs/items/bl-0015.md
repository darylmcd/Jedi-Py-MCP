# bl-0015 — manifest.json invalid for MCPB v0.3

**row:** `bl-0015` · **pri:** `Medium` · **size:** `M`

## Anchors

- `manifest.json`
- (new) `scripts/validate_manifest.py`
- `justfile`
- `scripts/bump_reinstall.py`
- (new) `tests/unit/test_manifest.py`

## Acceptance

- [ ] manifest passes an MCPB v0.3 schema check run by `just ci`
- [ ] `scripts/bump_reinstall.py` version sync still works on the restructured file

## Evidence

- `author` and `repository` are strings, `server` missing, `tools` holds launch config instead of a tool list, `categories` undefined, no runtime/env declared. — see `audit-reports/20260923-2310_jedi-py-mcp_mcp-surface-audit.md` and `ai_docs/audits/20260923-2310/`

## Context

- Repo is public and CHANGELOG advertises the manifest as directory compliance.
