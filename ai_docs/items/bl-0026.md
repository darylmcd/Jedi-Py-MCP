# bl-0026 — Directory-typed tool params skip the workspace-boundary check

**row:** `bl-0026` · **pri:** `Medium` · **size:** `S`

## Anchors

- `src/python_refactor_mcp/tool_runtime.py`
- `tests/unit/test_error_boundary.py`

## Acceptance

- [ ] Every directory-typed tool parameter is validated against the resolved workspace
- [ ] A unit test rejects an out-of-workspace directory argument

## Evidence

- `tool_runtime.py:32-39` `PATH_PARAMS` lists file/source/root/destination params only — no `*_dir`. bl-0007 bounded `create_type_stubs.output_dir` locally; other folder params are unchecked.

## Context

- Spin-off from /backlog-remediate plan `20260924T131340Z_backlog-remediate` (closeout, evidence re-verified at HEAD `20b565c`).
