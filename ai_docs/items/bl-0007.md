# bl-0007 — create_type_stubs reports success without output

**row:** `bl-0007` · **pri:** `High` · **size:** `M`

## Anchors

- `src/python_refactor_mcp/backends/pyright_lsp.py:1389-1410`
- `src/python_refactor_mcp/tools/analysis/type_stubs.py:20-26`
- `tests/unit/test_pyright_lsp.py`

## Acceptance

- [ ] `create_type_stubs{package_name:"parso"}` produces `.pyi` files and returns their paths
- [ ] an unknown package name returns an error, not `true`
- [ ] description states where stubs are written and that the tool writes immediately (no preview)

## Evidence

- Tool returns `{"result": true}` for `parso` and for `"not an identifier!!"`; no `typings/` or `output_dir` content is created; raw `pyright.createtypestub` answers `result:null` for every argument shape tried. — see `audit-reports/20260923-2310_jedi-py-mcp_mcp-surface-audit.md` and `ai_docs/audits/20260923-2310/`

## Context

- Success is currently "no JSON-RPC error"; `output_dir` is passed in the position Pyright uses for the import name.
