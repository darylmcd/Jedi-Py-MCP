# bl-0004 — prepare_rename always returns null

**row:** `bl-0004` · **pri:** `High` · **size:** `S`

## Anchors

- `src/python_refactor_mcp/backends/pyright_lsp.py:1177-1225`
- `tests/integration/test_end_to_end.py:330-348`
- `tests/unit/test_pyright_lsp.py`

## Acceptance

- [ ] `prepare_rename` on the `User` class name in the integration fixture returns its identifier range + placeholder `User`
- [ ] unit test feeds each of the three LSP reply shapes (Range, {range,placeholder}, {defaultBehavior}) through the parser
- [ ] integration test no longer accepts `None` as success

## Evidence

- Raw `textDocument/prepareRename` → `{"start":{…},"end":{…}}`; tool → `null`, while `rename_symbol` at the same position succeeds. — see `audit-reports/20260923-2310_jedi-py-mcp_mcp-surface-audit.md` and `ai_docs/audits/20260923-2310/`

## Context

- `isinstance(result, dict) and "range" not in result` treats a bare Range (a dict without a `range` key) as `defaultBehavior`.
