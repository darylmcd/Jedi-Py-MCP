# bl-0034 — Pyright position/file preflight reported as backend outage

**row:** `bl-0034` · **pri:** `High` · **size:** `S`

## Anchors

- `src/python_refactor_mcp/backends/pyright_lsp.py:405-417`
- `src/python_refactor_mcp/backends/pyright_lsp.py:572-600`
- `tests/unit/test_pyright_lsp.py`

## Acceptance

- [ ] `get_type_info{line:-1}` (and any out-of-range line/character) returns `[INVALID_INPUT]` naming the parameter, with no "check server_status and retry" advice
- [ ] a nonexistent `file_path` returns `[INVALID_INPUT]` naming `file_path`
- [ ] unreadable-file / missing-cache failures keep the redacted `[PYRIGHT_BACKEND] … Failure ID` shape

## Evidence

- `_validate_position` (`pyright_lsp.py:585-600`) and `ensure_file_open` (`:414-415`) raise `PyrightError`, which the tool boundary redacts to "Type-analysis backend failed; check server_status and retry". Audit: 397 caller mistakes answered with backend-outage text. — see `audit-reports/20260923-2310_jedi-py-mcp_mcp-surface-audit.md`

## Context

- Split from `bl-0003` (plan 20260924T183010Z_backlog-remediate). `ToolInputError` (errors.py) and `run_in_thread` passthrough already shipped (bl-0002/bl-0012).
