# bl-0021 — Closed-set tool params expose no enum or bounds

**row:** `bl-0021` · **pri:** `Low` · **size:** `M` · **deps:** `bl-0020`

## Anchors

- `src/python_refactor_mcp/tool_registry.py`
- `src/python_refactor_mcp/server.py`
- `src/python_refactor_mcp/models.py`
- `tests/contract/test_mcp_protocol.py`

## Acceptance

- [ ] `direction`, `style`, `kind`, `severity_filter`, `source`, `language`, `SignatureOperation.op` expose `enum` in `inputSchema`
- [ ] position `line`/`character` params advertise `minimum: 0`
- [ ] an out-of-set value returns a validation error naming the parameter

## Evidence

- Split from `bl-0017` (2026-09-24 /backlog-remediate `20260924T131340Z`): no parameterized tool (0 of 101) carries parameter descriptions; 98 of 101 silently accept unknown keys. — see `audit-reports/20260923-2310_jedi-py-mcp_mcp-surface-audit.md` and `ai_docs/audits/20260923-2310/`

## Context

- Parent `bl-0017` (Tool schemas under-specified) split into bl-0020..bl-0022, one regression shape each. Anchors: `tool_registry.py:186` (source), `:227` (severity_filter), `:359,:402` (direction), `:788` (style), `:1202` (language); `models.py` `SignatureOperation.op` + `_VALID_SIGNATURE_OPS`. Rejecting out-of-set values that were silently accepted is a behavior change on a PUBLIC repo — use a `changed-breaking-*` fragment. Shares files with bl-0020; order after it.
