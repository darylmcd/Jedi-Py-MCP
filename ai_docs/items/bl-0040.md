# bl-0040 — _validate_position compares UTF-16 columns against code-point length

**row:** `bl-0040` · **pri:** `Low` · **size:** `S`

## Anchors

- `src/python_refactor_mcp/backends/pyright_lsp.py:630-640`
- `tests/unit/test_pyright_lsp.py`

## Acceptance

- [ ] A position whose UTF-16 `character` equals the line's UTF-16 length (e.g. end of `other` in `s = '\U0001f600' + other`) is accepted
- [ ] The bound reuses the UTF-16 helpers added by bl-0033

## Evidence

- `pyright_lsp.py:634-635` at HEAD `9bcf000`: `line_length = len(lines[line])` / `if char > line_length:` counts code points while LSP `character` is UTF-16 units, so valid end-of-line columns after an astral character are rejected.

## Context

- Spin-off from /backlog-remediate plan `20260924T183010Z_backlog-remediate` (bl-0033 cold review).
