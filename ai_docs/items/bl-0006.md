# bl-0006 — generate_code fails for every kind

**row:** `bl-0006` · **pri:** `High` · **size:** `S`

## Anchors

- `src/python_refactor_mcp/backends/rope_backend.py:981-1012`
- `tests/unit/test_rope_backend.py`

## Acceptance

- [ ] preview for `kind="function"` at an unresolved call returns a stub-definition edit
- [ ] each of `class`, `function`, `variable`, `module`, `package` is exercised against real rope (not mocked)
- [ ] no `# pyright: ignore[reportAttributeAccessIssue]` remains in the generator table

## Evidence

- Every call returns `[ROPE_BACKEND]`; direct backend call: `AttributeError: module 'rope.contrib.generate' has no attribute 'create_class'`. — see `audit-reports/20260923-2310_jedi-py-mcp_mcp-surface-audit.md` and `ai_docs/audits/20260923-2310/`

## Context

- rope 1.14 exposes `create_generate(kind, project, resource, offset)`, `create_module(project, name, sourcefolder)`, `create_package(project, name, sourcefolder)`. The dict literal raises before `kind` is consulted, so module/package fail too.
