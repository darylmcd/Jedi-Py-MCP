# bl-0028 — Import-root and relative-import helpers duplicated across metrics modules

**row:** `bl-0028` · **pri:** `Medium` · **size:** `M`

## Anchors

- `src/python_refactor_mcp/tools/metrics/architecture.py`
- `src/python_refactor_mcp/tools/metrics/dependencies.py`
- `tests/unit/test_metrics_tools.py`

## Acceptance

- [ ] Both modules use one shared helper for import roots, module/package parts and relative `ImportFrom` resolution
- [ ] A test shows a relative import inside a package `__init__` module resolves against that package in the dependency tools

## Evidence

- `architecture.py:67` `_layer_import_roots` copies `dependencies.py:82` `_import_roots`; the bl-0011 copy handles `__init__.py` package parts, the dependencies copy returns `module_parts[:-1]`. Flagged by the bl-0011 cold review.

## Context

- Spin-off from /backlog-remediate plan `20260924T131340Z_backlog-remediate` (closeout, evidence re-verified at HEAD `20b565c`).
