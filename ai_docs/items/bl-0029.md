# bl-0029 — create_type_stubs timeout/kill path untested

**row:** `bl-0029` · **pri:** `Low` · **size:** `S`

## Anchors

- `tests/unit/test_create_type_stubs.py`

## Acceptance

- [ ] Hung subprocess past a patched `_CREATESTUB_TIMEOUT_SECONDS` raises `PyrightError` and the process is killed and reaped

## Evidence

- `type_stubs.py:68-69` `wait_for(process.communicate(), timeout=_CREATESTUB_TIMEOUT_SECONDS)` / `except TimeoutError` — every unit test monkeypatches `_run_pyright_createstub`. Flagged by the bl-0007 cold review.

## Context

- Spin-off from /backlog-remediate plan `20260924T131340Z_backlog-remediate` (closeout, evidence re-verified at HEAD `20b565c`).
