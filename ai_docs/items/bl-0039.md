# bl-0039 — Pyright restart-retry leaks a raw TimeoutError

**row:** `bl-0039` · **pri:** `Medium` · **size:** `S`

## Anchors

- `src/python_refactor_mcp/backends/pyright_lsp.py:228-251`
- `tests/unit/test_pyright_lsp.py`

## Acceptance

- [ ] A timeout on the post-restart retry raises `PyrightError("... timed out after ...")` with the same message shape as the first attempt
- [ ] A unit test with a stub client that dies, then hangs on retry, covers it

## Evidence

- `_request` (`pyright_lsp.py:228-251` at HEAD `9bcf000`): the first `asyncio.wait_for` is guarded by `except TimeoutError as exc:` (`:237`), but the retry `return await asyncio.wait_for(` at `:247` sits inside `except PyrightError:` (`:241`) with no timeout wrap, so a raw `TimeoutError` escapes. Likely source of the bare `TimeoutError` in the bl-0030 gate evidence.

## Context

- Spin-off from /backlog-remediate plan `20260924T183010Z_backlog-remediate` (bl-0030 deepener + cold review).
