# bl-0033 — LSP positions treated as code points instead of UTF-16 units

**row:** `bl-0033` · **pri:** `Low` · **size:** `S`

## Anchors

- `src/python_refactor_mcp/backends/pyright_lsp.py`
- `tests/unit/test_pyright_lsp.py`

## Acceptance

- [ ] A line with an astral character before the identifier yields the correct range and placeholder

## Evidence

- `pyright_lsp.py:1222` `anchor = char if is_identifier_char(char) else char - 1` indexes `line_text` by Python code point; LSP `character` is UTF-16. Flagged by the bl-0004 cold review.

## Context

- Spin-off from /backlog-remediate plan `20260924T131340Z_backlog-remediate` (closeout, evidence re-verified at HEAD `20b565c`).
