# bl-0011 — check_layer_violations false-clean on dotted patterns

**row:** `bl-0011` · **pri:** `Medium` · **size:** `S`

## Anchors

- `src/python_refactor_mcp/tools/metrics/architecture.py:67-140`
- `tests/unit/test_metrics_tools.py`

## Acceptance

- [ ] `layers=[["pkg.backends"],["pkg.tools"]]` reports the same violations as component patterns `[["backends"],["tools"]]`
- [ ] `import a, b` checks both names
- [ ] a layer pattern that matches no scanned module is reported, not silently ignored

## Evidence

- Dotted patterns → `items:[]` with `files_scanned:137`; component patterns with the same ordering report violations. — see `audit-reports/20260923-2310_jedi-py-mcp_mcp-surface-audit.md` and `ai_docs/audits/20260923-2310/`

## Context

- `_get_layer` tests `pattern in parts` (single component); the Import loop keeps only the last alias; relative imports are skipped.
