# bl-0024 — AutoImport bad-output raises ValueError instead of RopeError

**row:** `bl-0024` · **pri:** `Low` · **size:** `S`

## Anchors

- `src/python_refactor_mcp/tools/refactoring/imports.py`
- `tests/unit/test_refactoring_tools.py`

## Acceptance

- [ ] Invalid/unsupported rope AutoImport statements raise `RopeError`
- [ ] A unit test pins the error class

## Evidence

- `imports.py:71` `raise ValueError(f"Rope returned an invalid AutoImport statement: …")` and `:78` (unsupported statement) — backend output, not caller input. Found while deepening bl-0002.

## Context

- Spin-off from /backlog-remediate plan `20260924T131340Z_backlog-remediate` (closeout, evidence re-verified at HEAD `20b565c`).
