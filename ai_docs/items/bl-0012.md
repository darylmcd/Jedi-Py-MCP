# bl-0012 — argument_default_inliner keeps the default

**row:** `bl-0012` · **pri:** `Medium` · **size:** `M`

## Anchors

- `src/python_refactor_mcp/backends/rope_backend.py:98-101`
- `src/python_refactor_mcp/server.py:210-221`
- `tests/unit/test_rope_backend.py`

## Acceptance

- [ ] preview of `argument_default_inliner` on `def f(a, b=2)` index 1 removes `= 2` and inlines `2` at call sites that omit `b`
- [ ] `index:-1` returns an input error

## Evidence

- Preview still contains `def helper(a: int, b: int = 2)`; `index:-1` accepted and Python-indexed. — see `audit-reports/20260923-2310_jedi-py-mcp_mcp-surface-audit.md` and `ai_docs/audits/20260923-2310/`

## Context

- Alternatively correct the description — but the removal is the documented contract.
