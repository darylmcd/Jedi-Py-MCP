# bl-0045 — Pyright diagnostics failures read as clean files

**row:** `bl-0045` · **pri:** `Medium` · **size:** `M`

## Anchors

- `src/python_refactor_mcp/backends/pyright_lsp.py:545-546`
- `src/python_refactor_mcp/tools/analysis/diagnostics.py:122-125`
- `tests/unit/test_pyright_lsp.py`
- `tests/unit/test_analysis_tools.py`

## Acceptance

- [ ] A per-file publishDiagnostics wait that times out is reported to callers (a raised backend error or a `scan_failures` entry), not returned as `[]`
- [ ] The wait bound comes from config (e.g. `_request_timeout_seconds`), not the `2.0` literal
- [ ] `get_workspace_diagnostics` reports files whose fetch raised instead of silently dropping them
- [ ] Unit tests pin both behaviours

## Evidence

- `pyright_lsp.py:545-546` at HEAD `4c36c97`: `with contextlib.suppress(TimeoutError):` / `await asyncio.wait_for(event.wait(), timeout=2.0)` — a missed publish is indistinguishable from a clean file, and the bound is hardcoded.
- `diagnostics.py:122-125`: `all_results = await asyncio.gather(..., return_exceptions=True)` then `if isinstance(result, list): diagnostics.extend(result)` — exceptions are discarded with no signal.

## Context

- Spin-off from /backlog-remediate plan `20260925T141731Z_backlog-remediate` (bl-0009 deepener + executor measurement). Measured 0 timeouts on a 97/143-file corpus, so this is a correctness/visibility defect, not the bl-0009 perf lever.
