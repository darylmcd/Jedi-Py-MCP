# bl-0030 — Integration suite flakes on Pyright request timeouts under load

**row:** `bl-0030` · **pri:** `Medium` · **size:** `S`

## Anchors

- `tests/integration/test_end_to_end.py`
- `scripts/test-integration.ps1`

## Acceptance

- [ ] Root cause of the timeout failures identified and recorded
- [ ] The integration suite passes 3× consecutively under the gate's parallel load, or the flake is documented in `ai_docs/known-flakes.md`

## Evidence

- 2026-09-24 gate for PR #117: `test_get_type_info_returns_non_empty_type`, `test_rename_symbol_returns_edits_without_applying`, `test_find_constructors_finds_call_sites[analysis]` failed with `PyrightError`/`TimeoutError` (suite 230 s vs ~108 s normal); identical re-run passed. Investigate-first.

## Context

- Spin-off from /backlog-remediate plan `20260924T131340Z_backlog-remediate` (closeout, evidence re-verified at HEAD `20b565c`).
