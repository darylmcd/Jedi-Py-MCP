# bl-0013 — Preview hides file moves and creations

**row:** `bl-0013` · **pri:** `Medium` · **size:** `M`

## Anchors

- `src/python_refactor_mcp/models.py:273-281`
- `src/python_refactor_mcp/backends/rope_backend.py:834-976`
- `tests/unit/test_rope_backend.py`

## Acceptance

- [ ] `module_to_package` preview lists moving the module into a new package directory
- [ ] `move_module` preview lists the file move even when no importer needs rewriting

## Evidence

- `move_module` preview → 0 edits + "Moved module …"; `module_to_package` preview → one identity replace of the source file. — see `audit-reports/20260923-2310_jedi-py-mcp_mcp-surface-audit.md` and `ai_docs/audits/20260923-2310/`

## Context

- `RefactorResult` carries only `TextEdit`s; rope `MoveResource`/`CreateFolder` changes are dropped from preview.
