# bl-0042 — Workspace-relative directory resolution duplicated

**row:** `bl-0042` · **pri:** `Low` · **size:** `M` · **deps:** `bl-0041`

## Anchors

- `src/python_refactor_mcp/tool_runtime.py:176-181`
- `src/python_refactor_mcp/tools/analysis/type_stubs.py:42-50`
- `src/python_refactor_mcp/util/shared.py`

## Acceptance

- [ ] One helper in `src/python_refactor_mcp/util/shared.py` resolves a workspace-relative-or-absolute path and validates the boundary; both call sites use it
- [ ] Existing relative/absolute `output_dir` tests in `test_error_boundary.py` and `test_create_type_stubs.py` pass unchanged

## Evidence

- `tool_runtime.py:180` `candidate = requested if requested.is_absolute() else workspace_root / requested` duplicates `type_stubs.py:49` verbatim (HEAD `9bcf000`).

## Context

- Spin-off from /backlog-remediate plan `20260924T183010Z_backlog-remediate` (bl-0026 cold review). Consider folding into bl-0041's decision.
