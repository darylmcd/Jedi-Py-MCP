# bl-0041 — Relative path params resolve against the server cwd, not the workspace

**row:** `bl-0041` · **pri:** `Low` · **size:** `S`

## Anchors

- `src/python_refactor_mcp/util/shared.py:115-128`
- `tests/unit/test_error_boundary.py`

## Acceptance

- [ ] Decide and document the contract for a relative `file_path`/`root_path`: resolve against the resolved workspace root, or reject with `[INVALID_INPUT]`
- [ ] A unit test pins the chosen behavior independent of the process cwd

## Evidence

- `validate_workspace_path` (`util/shared.py:121` at HEAD `9bcf000`): `resolved = Path(file_path).resolve()` — a relative path resolves against the MCP server's cwd, so the same argument maps to different files depending on how the server was launched. bl-0026 resolves `output_dir` against the workspace instead, so the two path kinds now disagree.

## Context

- Spin-off from /backlog-remediate plan `20260924T183010Z_backlog-remediate` (bl-0026 planning). Investigate the MCP spec's expectation for relative paths before choosing.
