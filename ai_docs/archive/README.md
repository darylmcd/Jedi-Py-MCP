# Archive

This directory holds material that may still be useful for historical context, debugging, or future implementation work but should not remain in the active bootstrap reading path.

Put these here:

- one-off implementation prompts
- point-in-time audits or investigations
- completed rationale documents
- deep notes that are too noisy for active current-state docs

Do not treat archive contents as current policy unless an active canonical doc explicitly points back to them.

Current notable archive items:

- `python-refactor-mcp-prompt.md`: historical staged implementation prompt used as execution reference.
- `quickstart.md`: superseded bootstrap summary retained for historical context.