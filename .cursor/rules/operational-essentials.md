# Operational Essentials

Use this as a reminder layer only.

- Read `AGENTS.md` first.
- Follow the required doc read order from `AGENTS.md` without skipping.
- Check `ai_docs/runtime.md` before assuming tools or commands exist.
- Follow `ai_docs/workflow.md` for branch, worktree, and PR behavior.
- Use `CI_POLICY.md` as the single source for validation and merge-gate expectations.
- Use `ai_docs/README.md` as the project map for quick file routing.
- Keep changes scoped, validate what changed, and surface any skipped checks or risks.
- Do not duplicate policy here; update the canonical owner instead.